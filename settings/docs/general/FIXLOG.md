# 🐛 FIXLOG — 不具合の1行台帳（全プロダクト共通）

**使い方（WORKING-RULES 9章 ルールC）:**

1. **不具合の修正に入る前に**、このファイルを症状の言葉で検索する（例: 「真っ白」「回り続け」「変わらない」）
2. 同じ・似た症状が過去にあれば、**対処せず最初から原因調査モード**に入る（真因の列がヒント）
3. 不具合を直したら、**表の一番上に1行追記**する（新しいものが上）
4. 症状は**ヒデさんが言いそうな言葉**で書く（検索で引っかかるように。専門用語だけにしない）

| 日付 | プロダクト | 症状（ヒデさんの言葉で） | 真因 | 対処回数 |
|---|---|---|---|---|
| 2026-09-23 | anyflow v5 | スマホのキービジュアルのメッシュがやや右に寄って見える(錯視か？) | **錯視ではなく実際のズレ**。.orbit の SP centering(margin-left)を設計フレーム(915.483×0.53)の中心で寄せていたが、実際のケージ描画中心がフレーム中心より右にあり、ケージ＋惑星(惑星はケージ中心と一致)が画面中央195より **+4.9px 右**になっていた。→ SP の margin-left に -4.9px の中央化補正(isMobile内=PC不変)。⚠️ 測定のコツ: 最初 .orbit の全SVG要素で測ると画面外の薄い軌道楕円(-6.5)に引っ張られて「左」と誤判定した。可視要素(opacity>0.05)だけで測ると +4.9右が出た。実測: 補正後 ケージ195/惑星195(off 0)・PC margin-left 0・エラー0 | 1 |
| 2026-09-22 | anyflow v5 | 調整パネルでスマホのフォントサイズを変えると「PCとSPが連動する／入力の数字と実サイズが違う／上下ボタンで別の数字(16→12)から変わる」 | `textTools.applyAll` が **スマホモード(phone-mode)中に editsMb を親ページ(=PC表示)にも重ねていた**のが根因。①PCページの文字がSP値に化ける→「連動して見える」(実測: 導入事例 親PC44px→編集で21pxに化けた。ただし edits(PC本体)は無変更＝データは独立) ②パネル入力はiframe(SP)基準なのに親はPC基準→「入力≠実サイズ」に見える(Use Case 入力16 vs 親PC20) ③cs()がiframe未ロード時に親(PC)値を拾い、入力がPC値へ飛ぶ→スピナーが変な値から。スライダー(params.mb)は元からisMobile限定なのにテキストだけphone-modeでも重ねる不整合だった。→ ①applyAllの`_isMb`を実機isMobile限定に戻す=PCページは常にPC。SPプレビューは右下iframe枠と実機が担う(スライダーと挙動統一) ②cs()はスマホモード中iframeのみ・未ロードならnull、sync()はnullなら入力を上書きしない。実測(phone-mode): 編集で親PC=44px不変/入力=iframe(SP)一致/edits無変更。実機SP各セクション正常・エラー0。全56フォント行=共通経路で同時改善。⚠️ スマホモードのプレビューは「右下の実機枠(と実機)」で見る(PCページは変わらない仕様に) | 1 |
| 2026-09-22 | anyflow v5 | スマホで開発者体験(黒)あたりから動作が「のっそり」する／PCが発熱 | 2つの無駄な常時負荷。①`renderFrame` が KV グラフィック(WebGLのsphere描画＋軌道SVG＋ドットの毎フレーム setAttribute＋net3d 物理)を**画面外(実績/開発者体験/お問い合わせ)でも毎フレーム回していた**＝continuous GPU/CPU。②開発者体験のモックパネル `.dm-panel` は地色 #161616(不透明)なのに `backdrop-filter: blur(75px)` が付き、**見えないのにGPUが毎フレーム全画面75pxブラーを計算**していた。→ ①#sphere の画面位置で判定し画面外なら KV ブロックを丸ごとスキップ(elapsed駆動なので戻れば復帰・convLastTリセットで dt 飛び吸収) ②backdrop-filter 撤去(すりガラス案は別要素.dm-backのfilterなので不変)。実測(SP390): dev の最悪フレーム時間 19.8→**10.4ms**(スパイク消滅)・KV往復復帰OK・見た目不変・エラー0。⚠️ canvas/WebGLアニメは「画面外で止まっているか」を常に確認(vfDome/cvCanvasは既にゲート済みだった) | 1 |
| 2026-09-22 | anyflow v5 | 開発者体験(黒背景)に入ってヘッダーが白反転するのが「ワンテンポ遅い」 | 反転トグルが **#dev の上端が来る幾何条件(dr.top<=44)** だったため、背景(devDarkK)が先に暗転しきっても反転が devTop=0 まで待っていた(実測: 背景 devDarkK=1.0 は devTop=420 で到達、反転は devTop=0＝**約480px遅れ**)。→ 反転の開始条件を「実際の背景の暗さ devDarkK が 0.5 を超えた瞬間」に変更(退場は従来の dr.bottom>=30 を維持)。PC/SP共通トグル。実測: 反転ONと devDarkK>=0.5 到達位置が SP/PC とも一致=**遅れ 0px**、反転の瞬間 logoW=1・エラー0 | 1 |
| 2026-09-22 | anyflow v5 | 実績セクションの罫線のアニメーション(左→右に出る線)がスマホだと消えて最初から全部出ている | サイト軽量化で SP は `rfx-flow`＋`--rfx-hr/--rfx-vline=1固定`＋`transform:none` になり、罫線3本が最初から静的表示だった(PCのスクロール駆動アニメはそのまま)。→ SPだけ IntersectionObserver で各罫線がビューポート入場時に scaleX(0→1・左origin＝左から右へトリム)、3本は 0/0.12/0.24s ディレイ。inline!important で CSS の transform:none !important に勝つ。PCは inline未設定で無傷。実測(SP): 入場前 scaleX0・入場後 scaleX1・エラー0 | 1 |
| 2026-09-21 | anyflow v5 | ビジョンのメッシュ上の機能名(SDK等)の位置を上げるつまみ「全機能 Y」が効かない/変わらない | **つまみは実際は効いていた**。私の切り分けが誤りで、ラベル位置は `applyVfFade()` が `el.style.top` に書くのに、テストで `vfDraw()`(canvas描画のみ・ラベルには触らない)を呼んで「0px＝効かない」と誤判定していた。加えて SP は #vfWrap 内部が約0.56倍で描かれ、labGY 1単位＝画面0.56px しか動かないため小さい値だと変化が地味に見える(実測: labGY+100 で SP 56px/PC 71px 移動)。→ 位置の検証は必ず `applyVfFade()` を呼んで getBoundingClientRect で実測する。今回の依頼(SDK上面を矢印先端へ)は SP 持ち上げ `_mbLabGY` 既定 -38→-120(画面約-46px)で SDK上端 260→215px=メッセージ直下に着地。PCは持ち上げ撤去で 310px 不変=PC/SP独立。⚠️ メッシュのラベル位置を調べる時に vfDraw を見ても動かない(applyVfFade が本体) | 1(切り分けの誤りを訂正) |
| 2026-09-21 | anyflow v5 | ビジョン/KVのメッシュ形状(横長・ひし形など)がPC/SPで連動する／KVとビジョンでも分けたい | メッシュ調整スライダーは PC/SP 別の住み家(ビジョン=dome/domeMb・KV=conv.mesh/params.mb)を持つのに、**形状適用 visApplyMeshShape/kvApplyMeshShape が共有ベース(v.dome / conv.mesh)へ直書き**してSP機構を迂回していた＝形状だけ連動。→ 形状の中身(msx/msy/msz/mpinch)を各セクションのSP経路で書く(ビジョン=vfSet／KV=params.mb+applyMbToParams)、選んだ形状キーもPC/SP別に保存(vision.meshShape/meshShapeMb・conv.mesh.meshShape/params.mb)。KV↔ビジョンは住み家が別(vision.dome vs conv.mesh)で元々独立。実測(実関数＋phone-modeトグル): ビジョン PC横長(1.32)×SP縦長(0.84) / KV PC横長×SPひし形 で独立・エラー0 | 1 |
| 2026-09-21 | anyflow v5 | ビジョンのメッシュを「濃く」「回転させる」と何回直しても効かない（回転も止まって見える） | **保存値がコード既定を隠していた**のが真因。メッシュの色/濃さ/回転(lineColor/lineAlpha/spin/spinOn)を VF_DEF で変えても、ヒデさんのブラウザには過去の保存値 `vision.dome`(フルオブジェクト)と `gfxVarOverride.visMesh` が残っていて、vfCfg の合成(VF_DEF←dome←domeMb)で**保存値が必ず勝つ**ため反映されなかった。回転は spinOn=1 で実際は回っていた(Playwrightで elapsed 進行＆canvas変化を確認)が、遅く(spin0.35≒2.4°/s)＋対称球で静止に見えていた。→ ①VF_DEF を濃く・速く(#8C8C8C→#5F5F5F / 0.6→0.85 / spin0.35→0.6) ②**一度きりの移行**で保存値(vision.dome/domeMb/over.visMesh)からこの4キーだけ剥がし、コード既定が勝つように。実測(実際の古い保存値で2回リロード): 実効 spin0.6/#5F5F5F/0.85 で安定・剥がれ確認・エラー0。⚠️ 今後もメッシュ既定を変えたら「保存値に隠されてないか」を必ず確認 | 複数回目・今回は真因(保存値の隠蔽)から根治 |
| 2026-09-21 | anyflow v5 | 導入事例のスマホビューで、フォントサイズが40pxと入っているのに見た目は40pxに見えない | パネルの文字サイズ欄が **SPでは使われない PC値(edits.fs) をそのまま表示**していた。applyAll は SP(isMobile/スマホモード)で base(edits)の fs を無視し editsMb.fs か CSS 既定で描くのに、パネルの sync は base.fs を出していたので「40と書いてあるのに20で描画」のズレ。→ sync の fs 表示を SP では base.fs を継がず「SP専用(editsMb.fs)か実描画サイズ」にする(太さ/行間/字間は共通なので従来どおり)。実測: edits.fs=40 設定でも SP パネルは実描画20pxを表示＝一致。全56行で パネル表示=実描画 を確認 | 1 |
| 2026-09-21 | anyflow v5 | ビジョンの「データをつなぐことが〜」の文字サイズが、デフォルト案と強調案で連動してしまう（何度直しても直らない） | **経路違いが真因**。サイズを変える口が2つあり、案ごとに独立していたのは「文字サイズスライダー(sections.vision.msgSize)」だけ。もう一方の「フォント行(🔤)」が書く `params.edits.visMsg.fs`(PC)／`editsMb.visMsg.fs`(SP)は**全案で共有の1つの入れ物**で、案切替時に何もリセットしていなかった。過去の修正はスライダー側(msgSize)だけを案別化していたため、ユーザーが実際に触るフォント行の連動は残り続けた。→ ①案別に控える対象へメッセージのフォント行(fs/lh/ls)を追加(`VAR_SNAP.visEmph` に `__vm`/`__vmMb`＋ヘルパー `visEmphGrabMsg`/`visEmphPutMsg`) ②案を切り替える瞬間に msgSize系＋visMsgのfs/lh/lsを素の既定へ戻す(setSel)→控えのある案だけ独立値を重ねる。実測(実ボタンクリック): PC=default 96px→strong 未調整 70px(連動なら96)→default 復帰96 ／ SP=default 40px→strong 26px→往復維持。太さ・位置(fw/dx/dy)は共通のまま | 複数回目・今回は真因(経路違い)から根治 |
| 2026-09-21 | anyflow v5 | ハンバーガーのドロワー内「お問い合わせ」ボタン、ホバーして戻すとちらつく | CTAの流れるラベル(.cta-reel)はホバーで translateY 変形するが、`will-change` 未指定だとホバー時だけ合成レイヤーへ昇格→解除で1フレーム再ラスタライズ＝ちらつき。加えてドロワーCTAは白箱↔シアン＋文字色の反転が transition 無しで瞬間切替。→ ①`.cta .cta-reel` に `will-change: transform`(レイヤー常設) ②ドロワーCTAに `transition: background-color/color`(なめらか切替)。⚠️ 一過性のため実機での最終確認は要 | 1 |
| 2026-09-16 | anyflow v4 | 調整パネルに謎のピンクの線(オブジェクト)が入っている | ピン留めの別セクション(2026-09-16追加)の空の行 `.sw-favrow` がピンクの背景＋枠を持ち、ピンが0件でも見えていた。`hidden` を付けていたが、`.sw-row{display:flex}`(クラス指定)が UA の `[hidden]{display:none}` に勝つため消えず、中身ゼロで高さだけの**ピンクの細い箱=線**として残っていた。→ ①ピンクの背景・枠・角丸を撤去(素の入れ物に) ②`.sw-favrow[hidden], .var-favhead[hidden] { display:none !important }` で0件時は確実に消す ③見出しの色をピンク→グレー。実測: 0件で display:none・高さ0 / 1件ピンで背景transparent・border0・ピル1個 | 1 |
| 2026-09-15 | anyflow v4 | 導入事例とお問い合わせの境界線が気持ち悪い（色が始まる線が見える） | ぼかしのカーブが**後半に変化が集中**する形だった(終わりの20%で 58%→100%)。帯が255px あった頃は『帯の頭を隠す』ために正しかったが、帯を導入事例側へ逃がして約100px に短くした時点で、逆に**終わりが段差**になり「ここから色が始まる」線が見えていた(実測: 境目のいちばん急な彩度変化 7.1/4px)。→ 両端の傾きが 0 に近い台形カーブ(前後25%をイーズ・中央は直線／最大傾き＝直線の1.33倍)に置き換え。実測: いちばん急な変化 **7.1 → 4.7/4px**、彩度の立ち上がりも 0,0,1,3,12,28,51,68 → 0,0,2,16,35,51,66,68 とほぼ等間隔に。カード下端より上は無彩色のまま(色がカードにかからない)。⚠️ 境目の見え方はこれで2回目の対処。真因は**「ぼかしの長さが変わるとカーブの最適解も変わる」**ことなので、帯の長さを変えたらカーブも見直す | 2 |
| 2026-09-15 | anyflow v4 | 導入事例とお問い合わせの間の余白が、調整パネルをどういじっても詰まらない | 溶け込みの薄い帯(素の高さの44%＝約255px)が**お問い合わせの中**にあったため、帯のぶんは何をしても灰色のまま残る作りだった。「間隔」をマイナスにしても帯ごと上がるだけ、「見出しの上の余白」を減らしても帯は比例して縮むだけで、カード下端→色が出るまでは 243px から大きく動かなかった(実測)。→ **帯をお問い合わせの中から追い出す**: canvas を導入事例の下の空きへ上へはみ出させ(`--cv-rise`)、マスクの終点をはみ出し量に一致させる。空きが足りない時は帯の方を自動で詰めるので、**どんな設定でもお問い合わせは上端から色が乗る**。グラデの写り方はシェーダの uRise でセクション基準に補正(セクション内12点のRGB差 最大1＝ニュアンス不変)。実測: カード下端→はっきり色が出るまで **243px → 103px**(間隔0) / **8px**(間隔−95)。SP は従来どおり。⚠️ お問い合わせのパネルが効かない系はこれで3件目(縦幅・グラデの形に続く)。共通の真因は**「セクションの寸法が“素の高さ”から逆算される作り」**で、つまみの効果が別の場所で打ち消されること | 1 |
| 2026-09-15 | anyflow v4 | 下の段のカードをホバーすると、セクションの切れ目で影がスパッと切れる | 間隔をマイナスにするとお問い合わせが負のマージンで上に重なるが、その地色は**不透明**なので、カードの外に出た影(ct-lift は下22px＋ぼかし44px＝約66px)がそこで塗りつぶされていた。カードの「隠れない所まで自動制限」は**カード本体**を基準にしていたので、外にはみ出す影は守れていない。→ 重なり順を導入事例(#cases)のほうを上に(position:relative / z-index:1)。導入事例の地色は transparent なので、お問い合わせが隠れることはなく、影だけが上に落ちる。実測: 高さ700/768/900 × 間隔0/−80/−200(=−95に制限) の全組み合わせで影の濃度が段差なく地色へ戻る(修正前は間隔マイナスで必ず段差) | 1 |
| 2026-09-15 | anyflow v4 | お問い合わせのグラデーションの上の部分が少し冷たい | 上の帯は①「白飛びの頭打ち」(90%)で淡い色が**無彩色の灰**(0.9,0.9,0.9)に潰れる ②溶け込みのマスクで canvas がほぼ透明になり、地色 #E7E7E7(完全な無彩色)がそのまま見えている、の二重で色味が消えていた。ディザ側だけ暖色にしても②が効かないため、実測で R−B が −7→−6 とほぼ動かず。→ ①シェーダは頭打ちの**後**に暖色へ寄せる ②上端 0 から山なりに立ち上がる暖色の膜(.cv-warm)を重ねる。既定色 #ffe0cc は地色と輝度が同じ(231)なので明るさは変えず色味だけ暖かくなる。上端は 0 なので継ぎ目は出ない。実測: 上端の R−B −1→+3・上部 −7→+1・シアン帯は −50→−41 で温存。境目の段差 0(PC/SP とも) | 1 |
| 2026-09-15 | anyflow v4 | 導入事例とお問い合わせの間隔をマイナスで詰めると、上のセクション(カード)がトリミングされる | マイナスの間隔は負のマージンでお問い合わせを持ち上げる作りで、お問い合わせの地色が不透明なため、詰めすぎるとカードの上に重なって隠していた。→ 詰められる量を「導入事例の下に空いている分」(セクション下端−グリッド下端−8px)までに JS で自動制限。実測: −200 指定が −95(900px高)/−80(768px高) に丸められ、カードは隠れない | 1 |
| 2026-09-15 | anyflow v4 | お問い合わせの縦幅を調整パネルでいじっても見た目が変わらない | 一体型の「縦幅」は min-height で、内容が収まる素の高さ(≒670px)より小さい値では何も起きない(焼き込み値 416 は常に無効)。さらに増えた高さはフッターの上の余白に全部入る作りだった。→ つまみを「追加の高さ」(素の高さ＋α)に変え、内容をフッターより上の空きの中央に置いて上下均等に広がるように。実測: +200 で上下に +100 ずつ | 1 |
| 2026-09-15 | anyflow v4 | お問い合わせのグラデーションの調整パネル(形・中心・広がりなど)をいじっても変わらない | 「中心 横/縦・広がり・縦横比・回転」はシェーダの放射(uGMode 1)/線形(2)の分岐でしか使っておらず、既定の「カンプ」(uGMode 0・行列で t を出す)では読んでいなかった。→ カンプの明るい中心(0.78,0.12)を基準に、中心ずらし・回転・縦横比・広がりをカンプの分岐にも適用。既定値では見え方が変わらないように換算した | 1 |
| 2026-09-15 | anyflow v4 | お問い合わせの縦幅や上の間隔を伸ばすと、デザインが破綻してスクロールすると変に見える | 「伸ばしても上が薄くならない」対応で入れたグラデの縦引き伸ばし(uVK: 上端固定で下へ伸ばす)が原因。放射グラデの t が 1 を超えた領域は最後の色でクランプされるため、伸ばすほど**下半分が平坦なピンク**になっていた(縦1100で顕著)。→ グラデの引き伸ばしをやめ、セクションに素直に収める方式へ。上が薄くなる件は溶け込みのマスクを px 固定にした対応で解決済み。実測: 縦1100・間隔300 でも色が最後まで流れる／スクロール前後の寸法差 0 | 2 |
| 2026-09-15 | anyflow v4 | 導入事例の罫線が以前は横幅いっぱいだったのに切れてしまう | 設計幅1440のステージを中央に置く方式にしたため、縦が低い窓(高さで縮小率が決まる時)はステージの見た目幅が画面より狭くなり、ステージ基準の `width:100%` が画面幅に届かなくなっていた。→ fit() から `--bleed-w`(画面幅÷縮小率)と `--bleed-x`(中央に置くための左オフセット)を配り、画面幅いっぱいで見せたい要素(導入事例のグリッド・KVのロゴ帯)だけステージの外へ広げる。実測: 1000/1440/1920/1180 すべてで左0〜画面幅 | 1 |
| 2026-09-15 | anyflow v4 | お問い合わせページに飛ぶと、フォーム(iFrame)の読み込みが遅くてレイアウトシフトが起きる | HubSpot のフォームは iframe で後から挿入されるので、それまで置き場所の高さが 0。フッターが上に詰まった状態で描かれ、フォームが入った瞬間に下へ押し出されていた(実測 CLS 0.287)。→ ①完成後の高さを先に確保(実測 892/998/1013/1033px を画面幅で段分け) ②待機中は骨組みを重ねる ③preconnect＋head で async 取得 ④準備完了で確保を解除。実測 CLS 0.0011・フッターのズレ 0px | 1 |
| 2026-09-15 | anyflow v4 | Our Vision の見出し・グラフィックの位置がカンプからずれた(レスポンシブ化の後) | Point ブロックは `right:120px`＝ステージ基準で、ステージが画面幅まで広がっていた時代にパネルのドラッグ(p1X/p1Y・p2X/p2Y)で打ち消していた。ステージを設計幅1440に戻した時点でその打ち消しが効きすぎ、左に寄った。軌道も同様に noPinX/noPinY で微調整されていた。→ カンプ(16678:22983)の座標を実測して CSS の基準値に入れ直し、ドラッグずらしは 0 に戻す移行を追加。実測でカンプとの差 0px | 1 |
| 2026-09-15 | anyflow v4 | お問い合わせの縦幅や、導入事例との間隔を伸ばすと、上の方が薄いまま色味がついてこない | ①溶け込みの境界(マスク)が canvas 高さに対する**%**だったので、伸ばすほど「薄い帯」も比例して伸びていた(44%: 669px→294px が 900px→396px)。②「間隔」が margin＝色のない地色の帯だった。③グラデ自体も canvas の縦に引き伸ばされ、上端側の淡い領域が広がっていた。→ ①境界は「伸ばす前の高さ」基準の px に(パネルの%表示は維持) ②一体型は間隔を padding にして色の面に ③シェーダに uVK を渡し上端固定で下へ伸ばす | 1 |
| 2026-09-15 | anyflow v4 | 開発者体験モックの「透明なやつ」(リキッドグラス／クリアガラス)が透けてしまう。背景にブラーが掛かっていない | カルーセルの後ろのカード(.dm-ghost)は半透明(opacity .55/.38)＋3D変形(translate3d/rotateY)を持つので、ブラウザはそこを独立した描画面(バックドロップルート)にする。その内側の板に backdrop-filter を掛けても**後ろのカードは参照対象に入らない**＝ぼけずに素通しで、3枚ぶんの文字が重なって見えていた。実測で確定(opacity を 1 にする・will-change と preserve-3d を外す・isolation を足す、どれでも改善せず)。→ ①ガラスの下に地色(--dm-frost)を敷いて素通しを止める ②後ろのカードを実際に filter:blur(--dm-backblur)でぼかす。両方ともパネルで調整可 | 1 |
| 2026-09-15 | anyflow v4 | ウィンドウ幅を変えても動かない要素がある／広い画面でビジョンの見出しや軌道が左に寄る(レスポンシブ総点検) | KV・ビジョン・実績・導入事例のステージ幅を「画面幅÷倍率」で画面いっぱいに広げていた(--sw/--swp)。中身は設計px の絶対配置(left:126px 等)なので、広い画面ではステージだけ広がり中身は左に固定(1920 で −329px)。縦が低い横長の窓でも同じ(1512×828 で −73px)。→ PC は常に 1440 のステージを中央に置き、縮小時は「1440×倍率」で中央計算(margin-left / left = max(0, (100% − 1440px×倍率)/2))。実測: 全サイズでズレ 0 | 1 |
| 2026-09-15 | anyflow v4 | 調整パネルでネットワーク案(D 大きいケージ)の数値をいじって「これをデフォルトに設定」しても、リロード・焼き込みで元に戻る | 選択中のバリエーションに「⤓ いまの設定で上書き」の控え(gfxVarOverride)があると、起動時にその控えが再適用されて保存値に勝っていた(実測: 線の濃さ 0.25 で保存→再読込で 0.54)。save() で控えも同じ内容に更新するよう根治＋指定の数値を3か所(保存値・調整の控え・上書きの控え)へ一度だけ揃える migration | 1 |
| 2026-09-15 | anyflow v4 | 実績の案7(ID26)で「下段の横スクロールが機能せず表示されない」 | 場面表の欠陥。線が伸びきって文章が出る(0.54)のとほぼ同時に横移動(0.58)が始まり、SaaS を読む区間が 4%(約180px)しか無かった＝出た瞬間に去って「表示されない」に見えた。Chromium/WebKit/実機 Chrome/実ホイールでも要素は動いていたので再現に手間取った。場面表を詰め直し(読む区間 15%)・固定 560→620vh・到着面は横移動の途中から見える。保存値が旧既定なら migration で新既定へ | 2 |
| 2026-09-15 | anyflow v4 | 実績の For AI ピクトのアニメーションで、線(パス)が途中で途切れる | 線は vector-effect:non-scaling-stroke なので stroke-dasharray は画面px。トリミング(vTrim)が座標系(220)の長さで dash を作っていたため、ピクトを 220px より大きく出す案(300/325/340/420px)では表示の 75% などで切れていた。svg の表示倍率(画面幅÷220)を毎フレーム測って掛けるように | 1 |
| 2026-09-15 | design-gallery | 最近スクレイピングがうまくいっていない気がする／日本メディアのサイトが見れていない | スクレイピング自体は正常(sankou/webdesignclip/81web/muuuuu/s5styleは毎日成功・新規追加継続)。真因は表示側: 2026-07に一度だけ手動シードした"pickup"(868件、うち77%が海外AIサービス等でjpドメインは23%のみ)が、フィルターピルには出せない(2026-07-19に意図的に非表示化)のに、日々スクレイプしている6ソースと表示ではラウンドロビンで同じ1/7の重みで混ぜられ、カードの約7枚に1枚が海外の手動シード枠に食われて日本メディアが薄まって見えていた | 1 |
| 2026-09-15 | anyflow v4 | 案22で大きい数字が終点へ動く時にガクッとずれる（レイアウトシフト） | 見出しを1行→2行へクラスで切り替えた瞬間に上段の高さが82→114pxに伸び、線・下段・数値の終点(x113px)が跳んでいた。見出しを常に2行の絶対配置にし transform だけで動かす(行ごとの寄せで中央揃え→左揃えを連続に) | 1 |
| 2026-09-15 | anyflow v4 | 調整パネルの「上書きを解除」「既定へ戻す」で値が戻らない | load() の浅いコピー({...DEFAULTS.sections.results, ...保存値})で、保存に無い入れ子(fx21/hero 等)が DEFAULTS と同じオブジェクトを共有し、つまみを動かすと既定まで書き換わっていた。起動時に原本(DEFAULTS_PRISTINE)を控え、params を structuredClone で深く複製 | 1 |
| 2026-09-14 | anyflow v4・revealモック | 先頭のNo.02だけ背景色が違う | staged専用の白い背景レイヤーと--study-whiteの実行時指定を削除。他案と同じV4背景を透過。一覧12枚の背景#e7e7e7、各ステージ透明を実測。旧変数1を注入して2回再読み込みでも透明を確認。背景の専用保存値なし | 1 |
| 2026-09-14 | anyflow v4・revealモック | 水色の矢印が途切れ途切れ／元のスロットがない／実績が不揃い | 大きなSVGにnon-scaling-strokeを付けてトリミング長まで固定していた。トリミングはSVG倍率、線幅のみ逆倍率へ変更し連続描画。公開V4とS18/A21・スロット関数の同一性を照合し、モックでも元のリール描画を使用。実績は等幅3列・中央揃えへ統一。1280pxで中心間約344.47px。S18下線はユーザー指定で非表示 | 1 |

| 2026-09-14 | anyflow v4・revealモック | ピクトグラムが全く動かない | 詳細でもvalTOvへ一定の時刻を書き込み続けていたため停止していた。固定フレームはサムネイル用だけに限定し、詳細8案は実時間の時計を毎フレーム進める。古いrunning=false・速度0でも停止せず、設定自体は変更しない。No.01/08の両SVGの変化と2回初期化を確認 | 1 |
| 2026-09-14 | anyflow v4・revealモック | ピクトが粗く、形が重なって分かりづらい | SVG自体は元データ。S18の周期74%地点は閉じる途中で三枚が重なるため、縦スクロール案とサムネイルは開いた23%地点へ変更。図の不要なwill-changeを外し、実寸でベクター描画。線幅1.50pxと元の色・形を維持。1280px表示で確認 | 1 |
| 2026-09-14 | anyflow v4・revealモック | サムネイルが開いた実装と違う／一覧の情報が多すぎる | 汎用のSVG画像・別比率のサムネイルと詳細先頭が一致していなかった。実装のSVGと同じ描画処理から1280×720の表示を生成し、同じ進行度で開く。比較は3案・通し番号に整理、回数表示を撤去。PC・スマホで3案を一画面に表示 | 1 |
| 2026-09-14 | anyflow v4・revealモック | 最初の左右のピクトが近すぎる | No.01（旧P1）の初期中心が39%/61%だった。34%/66%へ変更。1280px幅で中心間281.6→409.6px、図の枠457.4pxは維持 | 1 |
| 2026-09-14 | anyflow v4・revealモック | どの案をいつ作ったか分からない | 案IDのみで提案回・日時の表示がなかった。旧Round 02とファイル記録から第1〜4回を復元し、TY1を第5回として登録。履歴・カード・下部ナビ・タブ名に共通管理データを使用。過去日時は復元した目安と明記 | 1 |
| 2026-09-14 | anyflow v4・revealモック | ピクトだけだと何を表すか分からない | 用途名が説明文側にしかなく、説明の出現まで非表示だった。ピクト側にも初期用途名を追加し、説明の出現とともに受け渡す。P1の初期画面でfor SaaS／for AI各20.33pxを確認 | 1 |
| 2026-09-14 | anyflow v4・revealモック | 比較用ナビが実績の数字に重なる | 画面下89.5%の数値中心と固定ナビの領域が重なった。新規案の小数値中心を82.5%へ上げて下余白を確保。1280×720のT5で数値下端633px、ナビ開始666pxを確認 | 1 |
| 2026-09-02 | presenter-notes | サムネイルが出ない/「関数じゃない」エラー（store.js更新が効かない） | store.jsがブラウザキャッシュされ古い版が使われた→vercel.jsonでno-cache＋`store.js?v=2`でバスト | 1 |
| 2026-09-02 | presenter-notes | カンペから「送る」を押してもスライドが動かない/プレビューが同期しない | Figma埋め込みへのpostMessage(NAVIGATE_*)の送信先originが間違い。`embed.figma.com`宛だと黙って無視される→`'*'`宛で解決（受信はclient-idだけで動く） | 1 |
| 2026-09-02 | presenter-notes | 編集エリア・保存ボタンが hidden にしても消えない | `hidden`属性が `.btn{display:inline-flex}`/`.notes-edit{display:flex}` に上書きされていた→ `[hidden]{display:none!important}` を先頭に追加 | 1 |
| 2026-08-30 | anyflow v3 | 導入事例カードが「1枚ずつ」に戻る（再発） | 焼き込み設定ダンプ内の旧cardGap値が、migrationフラグ済みでも復活する構造 | 3回目で根治 |
| 2026-08-30 | anyflow v3 | Visionメッセージの「下からマスクで出る」が消えた | 強み強調の旧str-goが overflow:visible を常時付与しマスクを無効化 | 1 |
| 2026-08-29 | anyflow v3 | 開発者体験2のデザインが「全く変わらない」 | migrationが localStorage へ書き戻し忘れ→リロード1回目だけ直って見え、2回目で保存値に復帰 | 3回目で根治 |
| 2026-08-29 | anyflow v3 | 「強み」のチカチカが見えない | 発火が早すぎて、文字が出きる前にアニメが終わっていた | 2 |
| 2026-08-28 | anyflow v3 | 案を選び直しても前の回転が残って回り続ける | 前の案の保存値（gyroMix/orbitSpin/spinUp）が残留 | 1 |
| 2026-08-28 | anyflow v3 | エコーの残像が全く見えない／つまみが効かない | 実装バグ3件（同日に3連発。エフェクト系は追加時の配線漏れが出やすい） | 3 |
| 2026-08-27 | anyflow v2 | 白画面になる | 「一時停止（直接編集）」の状態が保存され、次回起動で何も表示されない＝保存値の層 | 1 |
| 2026-08-25 | anyflow v2 | すりガラスが透ける・ボケない | Figma書き出しSVGの背景ブラーは`<img>`読込で完全無効 ＋ Chromiumバグ2件（transform/scaleがbackdrop-filterを殺す #993644 / #415354762） | 多数（2日間） |
| 2026-08-23 | 網走 v2 | パネルで直した値が本番に見えない | 古いブラウザ保存値が焼き込み値を上書き → 保存キーをv33に上げて破棄 | 1 |
| 2026-08-23 | 網走 v2 | メッセージ出現がガタつく | スクラブ駆動が原因 → しきい値＋時間トランジションに作り直し | 1 |
| 2026-08-19 | anyflow v1 | 保存（💾）が効かない | 保存まわりの重大バグ | 1 |
| 2026-08-18 | anyflow v1 | 修正したはずが画面に反映されない | ブラウザキャッシュ → HTMLをno-cache設定にして対策済み | 1 |
| 2026-08-15 | anyflow v1 | セクションが真っ白になる | 3回目でやっと真因＝範囲置換の巻き添えで背景CSSごと削除していた | 3 |
| 2026-08-15 | anyflow v1 | 実績→開発者体験の切り替わりに違和感 | 4回目でやっと真因＝暗幕の下で板が2枚動いていた | 4 |
| 2026-08-15 | anyflow v1 | 英字が5%詰まって見える | SF Pro **Display** になっていた（カンプ指定は **Text**）。目視では判別不可＝実測必須 | 1 |
| 2026-08-14 | anyflow v1 | 所定の位置でスクロールしても進まない（空スクロール） | セクションごとのピン留め区間の計算ズレ | 全セクションで発生 |
| 2026-08-12 | 網走 v1 | カルーセルがロックされて動かない（無限ループ） | 再ロック処理の設計 | 2 |

> 出典: 2026-08-30 バイブコーディング白書（8月の全560コミットの棚卸し）を初期シードとして作成。
> 以後は不具合を直すたびにここへ追記していく。
| 2026-09-02 | anyflow v3 | アトム型など複数リング案で「軌道の線の太さ」が無反応(★1/6/7/9等) | 複数リング描画(convDrawShape)がringWidthのみ参照でconvOrbitWidth()の掛け忘れ。swBaseに乗算して解決。サイズ側(convOrbitScale)は元から適用済みだった |
| 2026-09-02 | anyflow v3 | 編集モードの自由回転でXYZを動かすと粒・ドットが全部消える | kvReproのsetHiddenがdotsBackG/dotsFrontGごと非表示にして基本7個だけ再投影(アトム型は再投影も無し)が真因。ドット層は隠さない＋再投影廃止=粒は回転に連動しない仕様へ |
| 2026-09-03 | anyflow v3 | 開発環境dev2カルーセルの入れ替わり時に前面パネルが半透明になり背景が透ける | 前面に入るカードのopacityが左後ろ(0.55)→前面(1.0)へ線形補間で、中間で~0.77と半透明。暗くなる側だけ終盤ディレイの非対称だった。明るくなる側を最初の30%で不透明化する対称処理に。実測: 最前面カード最小opacity 0.775→0.998 |
| 2026-09-07 | presenter-notes | プレゼン（共有）ビューでページを送るとトークスクリプトのフレームまで映る | 送りに `NAVIGATE_FORWARD` を使っておりFigmaプロトタイプの流れに乗っていた（スライドの隣に置いてある原稿フレームへ進む）。検出済みスライド一覧の順で `NAVIGATE_TO_FRAME_AND_CLOSE_OVERLAYS` を直接送る方式へ変更。あわせて present 側の `ensureCard`（未知ノードの自動登録）を廃止＝一覧に原稿フレームが混ざるのを防止、さらに一覧外が表示されたら直前スライドへ引き戻す保険を追加 |
| 2026-09-07 | presenter-notes | カンペのプレビューと共有画面の中身がズレる（Figmaを直しても古いスライドのまま出る） | 原因2つ。①カンペのプレビューが「バックアップ画像があれば画像を使う」設計で、画像は撮った時点の絵なのでFigmaを直すと共有画面（ライブ）と食い違う ②`prefetchImages` が保存済みノードを常にスキップしており、画像が二度と更新されなかった。→①プレビューをライブのFigma埋め込み本軸に変更（画像は8秒無応答時の保険のみ）②ファイルの `lastModified` を控え、変わっていたら全画像を撮り直す |
| 2026-09-09 | anyflow v3 | スマホ(iOS実機)でロゴティッカーが止まる／消えたり出たりする | ①`animationPlayState` が `params.running`(古いlocalStorageで false になりうる)依存 → モバイルは常時 running。②`rv()` が毎フレーム opacity/blur/transform を inline に書き、1826px 幅のマーキー層に blur フィルタが重なると WebKit が合成レイヤーを落とす → スマホでは JS から触らず CSS(opacity:1 / translateZ(0))で固定。headless では再現しない＝実機固有 |
| 2026-09-09 | anyflow v3 | スマホでスクロールすると中身がガクッと動く（揺れ） | iOS Safari のアドレスバー開閉で innerHeight(と幅サブピクセル)が変わり、renderFrame/ResizeObserver/resize の3経路から fit() が再レイアウト。fit() 冒頭で「モバイル＆幅が±2px以内なら return」。scroll 演出の凍結(clock 固定)だけでは止まらなかった |
| 2026-09-09 | anyflow v3 | スマホで実績見出しが Point02 に重なる／ビジョンの絶対配置が section 高さで動く | `.pin-stage` が `top:50%+translateY(-50%)` の縦中央寄せで、中身(1350px)が 780px 箱より高いと上下にはみ出して前セクションへ食い込む。ビジョンの「+42px オフセット」も同根(高さ依存)。モバイルは `html.mb .pin-stage{top:0; transform:scale(var(--sp)); transform-origin:left top}` + `#results` は align-items:flex-start で上端固定 → 値＝カンプY−section上端で置ける |
| 2026-09-08 | anyflow v3 | ビジョンだけ本番が遅い/修正を何度出しても直らない | 真因=世代ガードは「保存世代<焼き込み世代」でしか発火せず、一度「現行世代のまま遅い値」で固まったブラウザは永久に直らない(実測:現行世代+npOrbitDur3.5注入→リロード2回でも3.5)。しかも速化移行のフラグは既に立ち再適用もされない。対策=ビジョン重要値の内容署名を持ち、SHIPPEDと食い違えば本番のみSHIPPED値へ同期+localStorageも書き直す(焼き込みが常に正・固まった環境も次回ロードで自動回復・localhost除外・手動調整は署名一致中は尊重)。世代バンプ不要 |
| 2026-09-09 | anyflow v3 | パネルで「メッシュの線の濃さ」を0.25にしても戻る／SHIPPED_SETTINGSに0.25を焼いても本番が0.5のまま | 真因=起動時 applyOverrideOnBoot→applyGfxVariant が、選択中の案の「上書き控え(gfxVarOverride)」または案定義の conv(lineAlpha:0.5)を再適用し、params/SHIPPED.conv.mesh の値を潰していた（案が持つ値の住み家は params 直下ではなく「案定義／上書き控え／調整の控え(gfxTweaks)」）。対処=上書き控え側(SHIPPED_PRESET_STATE.over と SHIPPED_SETTINGS.gfxVarOverride)の mode.lineAlpha を0.25に、DEFAULTS.conv.mesh.lineAlpha も0.25に統一。検証=メッシュ案6選択で0.25、古い保存値注入→リロード2回で維持 |
| 2026-09-09 | presenter-notes | スライドを並び替えるとトークスクリプト／発表画面が1枚目にリセットされる | 原因3つ。①エディターが常に `edGo(order[0])` で開いていた ②`syncFromFigma` が完了時に `edNode = null` して現在ページを捨てていた（再取得のたび・エディターを開くたびに発生）③`setActiveDeckId` が同じデッキでも `deck-changed` を放送し、発表タブの `location.reload()` とカンペの初期化を誘発（発表中にエディターを開くと再生画面が飛ぶ）。→ ①②見ていたページを `pn_lastpage_<deckId>` に記憶し、並びに残っていれば再開。③同じIDなら放送しない。あわせて端で反対側へ回るループ送りに変更（ライブ／画像モード／エディター共通） |
| 2026-09-10 | presenter-notes | エディター左のサムネイルがFigma修正後も古いまま更新されない | サムネは取得済み nodeId を thumbCache にセッションキャッシュし `need = ids.filter(id => !(id in thumbCache))` で二度と取り直さず、別ファイルの時しか捨てていなかった。Figma画像URLは修正で別ハッシュになるのに古いURLを指したまま。→ syncFromFigma で lastModified の変化を検知したら thumbCache を破棄し直後の loadThumbs で撮り直す（変化なしはキャッシュ維持で無駄打ちしない）。実測: v1→(Figma修正)→v2 に更新、修正なし再取得はv2維持 |
| 2026-09-14 | abashiri v3 | スポット詳細ページがスクロールできない | html/bodyがoverflow:hidden（演出サイト共通仕様）なのに詳細ページを通常フロー(min-h-dvh)で実装。検証もプログラムスクロールだったため見逃した | mainをh-dvh overflow-y-autoの自前スクロール容器に。検証は実ホイール入力(computer scroll)で行う |
| 2026-09-14 | abashiri v3 | イベントセクションが白背景でなく後ろの写真が透ける・縦書き見出しが見えない | CSS描画順: sticky兄弟(positioned)が静的sectionの背景より上に描かれ、白背景とh2が沈む(absolute imgだけ見えた)。computed styleは白でも描画は青=computed確認だけでは検出不能 | sectionにrelative z-10。検証はスクショの目視も併用 |

| 2026-09-15 | anyflow v4・reveal No.01 | 実績の20,000+と隣の数字が重なる | 等幅3列に対して固定比率の文字サイズが大きすぎた。No.01を専用モジュールへ分離、数字を6vw・最大94pxへ縮小。1280幅で76.8px、数字間132.8px。1461幅で87.66px、数字間150.5pxを実測。本文の入口と横移動後の着地、縦スクロール復帰も確認 | 1 |
| 2026-09-15 | abashiri v3 | スポット詳細の案2でページごとクラッシュ（This page couldn't load） | useTransformの入力レンジにマイナス値を渡していた（[start-seg, start+seg] で i=0 のとき負）。ブラウザのWAAPIが「オフセットは減ってはいけない」で例外→レンダラー停止。console errorを読むまで原因が見えなかった | 入力レンジを必ず0〜1に収め増加順にする。スクロール連動のuseTransformは範囲をクランプして書く |
| 2026-09-15 | abashiri v3 | 体験セクションのスクロール演出が最後まで再生されない | ①セクションがページ最後で「上へ抜けきる」までスクロールできず進捗が1に届かない ②framerのuseScrollにcontainerとtargetを併用すると進捗が0のまま動かない ③検証時はBrowserペイン非表示でrAFが止まり、計測値が初期値に固まって見える(スクショでフレームを起こすと正しい値になる) | ①sectionの外に余白を足す(中はNG) ②進捗はrAFで自前計測 ③rAF系の実測はスクショとセットで行う |
| 2026-09-15 | abashiri v3 | スクロール演出の見せ場がセクションを通り過ぎた後に来る | 進捗を「入り始め0→抜けきり1」で取っていたため、完成(1)が画面外へ抜けた時点になっていた | 0.5=画面中央ちょうど なので、progress/0.5 をクランプして「中央で完成しそこから保つ」カーブに変換 |
| 2026-09-16 | abashiri v3 | スマホのKVで「網走市観光サイト」の位置がPCとずれる | 作字SVGは471x390だが、スマホ側は415px幅の枠に直接置いていたため img の max-width:100%（Tailwind preflight）で415pxに切り詰められ、高さが390→343.6pxに縮小。相対座標は同じでも作字だけ約12%小さく、キャプションとの関係がずれた。PC側は中間のdivに入っていたため縮まなかった | PCと同じ入れ子構造（left:-28px の div の中に w-471/h-390 の img）に揃え max-w-none を明示。実測でPC・スマホ・タブレットすべて作字471x390・キャプション相対(215.7, 8.3)で一致 |
| 2026-09-16 | abashiri v3 | スマホの詳細ページで横スクロールが出る | 基本情報の表の値(dd)がflexの子で、flexの子は既定で「中身の最小幅より縮まない」(min-width:auto)。長いURL https://www.instagram.com/darchadaccha が1語扱いになり375px幅を31px突き破っていた。ページ上部の静止状態＋Browserパネル非表示(rAF停止)では再現しないため見逃していた | ddに min-w-0 と break-words をセットで付与。検証はPlaywright実描画で11ページ×3幅(375/390/768)×6スクロール位置=全33組の横はみ出し0を実測 |
| 2026-09-16 | abashiri v3 調整パネル | 案のピン留め・削除がリロードで消える | 古いバージョンの保存値を掃除する処理が 'tp:<キー>:v' の前方一致だったため、案の隠し/ピン留めを入れた 'tp:<キー>:variants' まで毎回消していた（保存はされるのにリロードで戻る） | 数字つきのバージョン鍵 :v\d+ だけを対象にする正規表現へ。実測でピン留め★1と削除1件がリロード後も残ることを確認 |
| 2026-09-16 | abashiri v3 調整パネル | 自動テストで本番の焼き込み tune-defaults.json が書き換わった | ローカルでは「これをデフォルトに設定」が /api/tune-save 経由で焼き込みファイルへ自動保存する。それを知らずにテストでボタンを押していた | ファイルを git checkout で復元。以降のテストではこのボタンを押さず、値の確認は TunePanel.instances[0].params を読んで行う |
| 2026-09-16 | abashiri v3（開発環境） | コードを直したのに画面に反映されない（古い案の一覧が出続ける） | 同じフォルダで `next dev` が動いている最中に `next build` を何度も実行していた。両方が .next を共有するため dev 側のキャッシュが古い状態で固まる。ソースは正しいのに、ブラウザからは version 4・案6件のまま見えていた | dev を止めて `.next` を消して dev を再起動（実測: version 5・案16件に復帰）。以後、dev が動いている間は同じフォルダで build しない。確認は dev 側で行い、build は dev を止めてから |
| 2026-09-16 | abashiri v3 フッター | 写真が繰り返し表示されているように見える／レイアウトが破綻 | 高さに dvh を使っていた。トップページは 1512×982 の紙を画面に合わせて縮小表示しているため、dvh と見た目の高さが一致しない（実測: 画面756pxに対し写真582px）。写真が画面を埋めきらず、下に地が出て繰り返しに見えた | スクロールしている箱の clientHeight を測って px で組む。進み具合も offsetTop と scrollTop（どちらも縮小前のpx）で出す。実測: ワイド2000×756・標準1512×982・詳細ページのいずれも写真が画面を縦横100%覆う |
| 2026-09-16 | abashiri v3 フッター | 写真の案でフッターの上端にうすい水色のすじが出る | 白いグラデーションの面を top:0 ぴったりに置いていたため、小数点のまるめで写真の上端が1pxだけのぞいていた | グラデーションを上に3pxはみ出させる（top:-3px・高さ+3px）。案1では出ず案6で出る、で切り分けて特定 |
| 2026-09-16 | abashiri v3 フッター | 作字のロゴが横に引き伸ばされて歪む（左右に白い棒が出る） | 縦並び(flex-col)の中に img を置くと、align-items の既定(stretch)で幅いっぱいに伸ばされる。w-auto は効かない。実測: 471×390 の作字が 1272×120 になっていた | img に object-contain を保険で付け、置き場所側に self-start を付ける |
| 2026-09-17 | abashiri v3 | 768px（縦タブレット）でスポット詳細の本文が1行10文字まで潰れる | 2カラム／12列グリッドへの切り替えを Tailwind の `sm:`（640px）で行っていた。768px では 左右パディング200px＋目次230px＋間110px＝540px が先に取られ、本文に 228px しか残らない。案37は190+90、案38は12列グリッドで同じ症状。スマホ(375/390)は1カラムなので気づけず、PC(1440)は余裕があるので気づけない「間の幅」だけで出る | 横組みへの切り替えを `lg:`（1024px）へ引き上げ、横パディングに `md:px-[56px]` の中間段を追加。実測(8案×8幅): 768px の本文幅 228→656px（案3は288→720px）、1024px以上は修正前と同値（案32 620px 等）で不変、横スクロールは全幅0 |
| 2026-09-17 | abashiri v3 | スマホのフッターに「網走市観光サイト」が無い | PCフッター(SiteFooter の Logo)にだけ入れて、スマホは MobileTop 側の別実装だったため入れ忘れ。さらに text-kanko-site.svg は【白い文字】で、スマホのフッターは白背景なので、そのまま置いても見えない | 青版 `text-kanko-site-blue.svg`(fill を #0070C9 に) を作り、PC と同じ 415x379 の枠組み（作字は入れ子divに逃がして max-w-none）で配置。実測: 作字に対する相対位置 dx=96.4 / dy=-1.9 は、設計値 (243.7, -4.8) × 倍率 0.396 と一致 |
| 2026-09-17 | Vercel 全体 | 網走を上げようとしたら「1日100デプロイ」の上限で弾かれる | **Vercel API で実測した内訳（直近24時間 87件）: 旅のしおり 45回（全部 CANCELED）／design-gallery 40回（38 CANCELED・2 READY）／網走v3 1回／houmon-app 1回。つまり 83件（95%）が1つも完成しないまま枠だけ消えていた。** 原因はモノレポ `hideyuki-yamanaka.github.io` に Git 連携しているプロジェクトが2つ（tabinoshiori→RootDir `travel-shiori` ／ design-gallery→RootDir `design-gallery`）あり、どちらも `ignoreCommand` 未設定だったこと。**自分のフォルダを1行も触っていない push でもビルドが始まり**、次の push に追い越されて CANCELED になる。直近24時間のコミットは106回（うち27回 Auto-save）あったので、その都度2件ずつ枠を食っていた | 両方に `vercel.json` を置き `ignoreCommand: git diff --quiet HEAD^ HEAD -- .` を設定（Root Directory が各フォルダなので「このフォルダに変更が無ければビルドしない」の意味になる）。実測: 直近8コミット（Auto-save・anyflow・docs）は両方とも「スキップ」、design-gallery を触った 209238d だけ「ビルドする」と判定 |
| 2026-09-17 | 旅のしおり | Supabase の延命(keep-alive)が毎日失敗し続けていた（少なくとも9/12〜9/16の5日連続） | /api/keep-alive が `fetch failed` を返す＝Supabase 側がもう止まっている。本体の最終更新は 2026-05-13（4ヶ月前）で、直す予定も無い。`fail-fast: false` の matrix なので houmon-app と nittei-chousei への巻き込みは【無かった】（実測: 同じ run で両方 ✓）が、ワークフローが毎日赤くなるため、本当に困った時のアラートが埋もれる | ヒデさん「ほぼ使っていない」との判断を受け、keep-alive の matrix から travel-shiori を外した。あわせて Vercel の Git 連携も解除（自動デプロイ停止・サイトとURLはそのまま生存 200）。また使う時は Supabase を Restore してから matrix に戻す |
| 2026-09-17 | design-gallery | 9/16 のスクレイピングが失敗し、新着147行ぶんが捨てられた | スクレイパーは実行に10分かかる。その間にヒデさん／Claude が main へ push すると、最後の `git push` が `! [rejected] (fetch first)` で落ちる。checkout 直後の状態のまま push していたため、相手の変更を取り込む処理が無かった。実測: 2026-09-16T04:02:03Z のログに rejected が残っていた（スクレイプ自体は成功、コミットも作れていた） | push を `git pull --rebase origin main && git push` の3回リトライに変更。作業が活発な時間帯ほど効く |
| 2026-09-17 | abashiri v3 | 縦向きの大きいタブレットで文字が9px相当になり、上下に350〜388pxの空き帯が出る | 固定キャンバスは「幅1512・高さ982」の【横長】なので、画面が縦長だと縮小率が幅で決まり(vw/1512)、高さが大きく余る。スマホ版への切り替えを【幅900px以下】だけで判定していたため、幅912〜1024の縦長タブレットがキャンバス版に流れていた。実測: Surface Pro 縦 912x1368=縮小0.603(本文9.0px)・帯388px ／ iPad Pro 13 縦 1024x1366=縮小0.677(10.2px)・帯350px | 判定に「縦長ならば1280pxまで」を追加（`(max-width:900px), (max-width:1280px) and (orientation:portrait)`）。そのうえで縦長用レイアウトが iPhone の組みのまま引き伸びるので、`--breakpoint-tab: 744px` を足してタブレット専用の組みを当てた。実測(744/820/1024px): KVロゴ 301→471px、スポット本文 294→504〜640px(行長を640pxで頭打ち)、体験の写真 150→300px、グルメの写真 300→430px、フッターロゴ 186→286px。横スクロールは全幅0 |
| 2026-09-20 | abashiri v3 | 詳細ページ案5で「写真は固定」にしたのに、白が覆いきる前に写真が動き出す | 貼りつき(sticky)が切れるのは「ステージ高 − 画面1つ」を過ぎた時。ステージ200dvh・画面1つ100dvh なので 100dvh 地点で切れるが、白い面が完全に覆うのは「乗り上げ開始100dvh ＋ グラデ46dvh」＝146dvh 地点。46dvh ぶん足りていなかった（実測: scrollTop 1200 で写真top 0→−462px、その時まだ白の覆いは83%） | ステージを 146dvh＋画面1つ＝246dvh 以上に。グラデを70dvhへ伸ばした後は 170dvh＋100dvh＝270dvh 必要なので 280dvh に。実測: 白の覆いが100%になる 1530px まで写真top=0 のまま |
| 2026-09-20 | abashiri v3 | 詳細ページの案32・35 でだけ JSエラー「Target ref is defined but not hydrated」 | 目次5案で共通の TocLayout に、案36用の `useScroll({target: stage})` を足した。stage は貼りつく形（案36）でしか描かれないので、案32・35 では中身の無い ref を渡していた。Hooks は条件分岐できないので呼び出し自体は消せない | target だけ条件付きに（`target: pinnedHero ? stage : undefined`）。実測: 8案×2幅の全16通りで JSエラー0 |
| 2026-09-20 | abashiri v3 | KVの「ぼーっとしてみる」→体験ページの遷移が不自然 | ①幕に backdrop-blur-22 を掛けて一度ぼかしていた ②幕の色（from-brand via-brand/70 to-sky-bottom）が体験ページの背景（Stage の brandOverlay＝from-brand via-brand/45 to-transparent）と別物で、幕が晴れた先で色が変わって見えていた | ヒデさん指定の「導入→場面選択と同じ」に合わせ、ブラーを廃止、幕を体験ページと同じ重ね方にそろえ、0.8→0.6秒に。遷移の待ち時間も600msへ。実測: 7コマ連写で背景の青が途切れずつながる |
| 2026-09-20 | abashiri v3 | サイト全体が「のっそり重い」 | **フォントが245ファイル・3.06MB 読み込まれていた**（本番トップで実測）。next/font は日本語フォントを1ウェイトにつき20前後のサブセットに割るため、Noto5＋Zen3＋Rounded4＋Inter3＝15ウェイトで爆発していた。うち **Zen Kaku Gothic New と M PLUS Rounded 1c は実装のどこからも参照されていなかった**（globals.css に --font-zen-body / --font-rounded-num の定義があるだけで、その変数を使う要素がゼロ）。v1.0 の名残 | app/layout.tsx から2書体の読み込みを削除。実測: フォント **245→36ファイル / 3.06→0.8MB**、総リクエスト **250→80件**、FCP **884→196ms**。書体は全4ページで Noto Sans JP と Inter のみ、フォールバック混入なし。見た目の実測値（ナビ44x18・本文18px/w456・見出し36px/w1440）は削除前と**完全一致** |
| 2026-09-20 | abashiri v3 | 画像が重い（トップ4.06MB） | ①ヒデさん提供の写真は**元が WebP** だったのに、こちらで JPEG に変換して置いていた。二重変換で無駄に太っていた（例: サンゴ草 350KB→614KB）。②スマホ版の場面選択にだけ網走駅が残っており、使わない 1.4MB を配信していた。③旧バージョンの名残の画像が11本、どこからも参照されずに残っていた | ①提供 WebP をそのまま配置し参照42箇所を .webp へ（**再変換しないので劣化ゼロ**）。②スマホ版の場面選択を PC版と同じ中身・並びにそろえ spot-eki.jpg を削除。③未参照の11本を削除。実測: img フォルダ **32MB→24MB**、トップの画像転送 **4.06→3.5MB**。全8ページ×2幅で画像404・壊れ画像・JSエラーすべて0 |
| 2026-09-20 | abashiri v3 | 体験セクション案2・3で、2枚目までしか見ないうちに下へスクロールできてしまう | 写真が流れる速さは「一定」（＝スクロールの強さに関係なく時間で進む）。一方、貼りつき(sticky)が切れる位置はスクロール量で決まる。**時間で進むものと位置で決まるものは、そのままでは両立しない**ので、強くスクロールすると写真が追いつく前に場面を抜けてしまう。加えて、貼りつき中の前半5割で1枚も剥がれない死に区間があった（実測: 進み0.5 まで0枚、0.95 でも2枚） | ①剥がれ始めを早め（0.08→0.02）、間隔を詰めて 0.96 までに3枚とも剥がれ終わるよう再配置 ②貼りつく長さ 3.6→4.8画面 ③一定速度 0.22→0.38 ④**「見せ終わるまで下へ行かせない関所」を新設**（PinStage の hold）。実測: 案31・32 × スクロール3段階（そっと/ふつう/一気に）の全6通りで「4枚目まで見えてから解放」。体験5案とも一番下まで到達でき、詰まりなし・JSエラー0 |
| 2026-09-20 | abashiri v3 | トップのぼーっとスポットで、左下のサムネを押しても切り替わらない | サムネの jumpTo が自前の rAF ループで `sc.scrollTop += dy*0.14` を書いていたが、TopPage の慣性スクロールも毎フレーム scrollTop を書くため取り合いになり、慣性側に戻されていた（実測: サンゴ草を押しても写真は能取岬のまま、スクロールは180pxしか動かず）。**GlobalNav の「ホーム」で 2026-08-23 に直したのと同じ事故**で、そちらは「行き先をイベントで渡して動かすのは TopPage 一本」に統一済みだったが、ここだけ旧方式のまま残っていた | jumpTo を `abashiri:scroll-to` イベントに置き換え。実測: サムネ3枚それぞれを押して、押した写真＝切り替わった写真が3/3で一致。残りの3枚も正しく入れ替わる |
| 2026-09-20 | abashiri v3 | 詳細ページの罫線がばらばら | 見出しの上に区切りの罫線を置き、基本情報の表にも上端・下端の線があった | ヒデさん指示でルール化：**①見出しの上下には罫線を入れない ②表は行と行の区切りだけ（一番上と一番下の線は無し）**。区切りは罫線ではなく余白（pt-[88px]）でつける。実測: 全8案で「見出し前後の罫線なし・表の上端線なし・表の下端線なし」を確認 |
| 2026-09-20 | abashiri v3 | 詳細ページの音声ON/OFFが左下に出る | SoundUi は `#abashiri-sound-slot` という置き場を探して、そこへ描く作り。**置き場が無いページでは `fixed bottom-4 left-4`（左下）にフォールバック**する。トップ・スマホ・体験ページには置き場があったが、詳細ページのテンプレだけ無かった | 詳細ページの共通ヘッダー(DetailHeader)に、トップと同じ座標(left:34 / top:32)の置き場を追加。実測: トップ(31,23) に対し詳細(34,25)。全8案とも同じ位置・JSエラー0 |
| 2026-09-21 | abashiri v3 | 詳細ページで、写真が最後まで固定されない | sticky（貼りつく）方式は「貼りつく区間」を持つため、区間を抜けると必ず写真が動き出す。区間の長さを伸ばしても、根本的に「いつか動く」ことは変わらない | position:fixed に変更。区間という考え自体を無くし、最後まで1pxも動かないようにした。⚠️ ここで fixed が使えるのは詳細ページが main を自前でスクロールさせているから（トップページのように body ごと拡大している画面では使えない）。実測: scrollTop 0〜5000 のどこでも写真は (0,0) 1440x900 のまま、白い面だけが 900→-4100 と動く |
| 2026-09-24 | abashiri v3 | 体験セクションで下にスムーズに行けない／スクロールバックで上に行けない／4枚目を見ても下に行けない | 前日に入れた「関所」が強すぎた。①上向き(`__abashiriScrollGateMin`)にも通せんぼしていた ②解除の条件が「進み具合 1.0」で、実際に4枚目が出る 0.96 より先だったため見終わっても閉じたまま ③解除されない状態から抜ける手段が無かった | 関所を作り直し。①上へ戻る時は一切止めない（上向きの関所は廃止）②4枚目が出た時点(0.96)で即あける ③保険の時間切れ(6秒)を追加。実測: 案31・32 とも「4枚見て下へ抜けた(4.5秒・剥がれ3枚)」「上へ戻れた(13440→5400)」 |
| 2026-09-24 | abashiri v3 | 体験セクションで「3スクロールぐらい無駄にしないと下に行けない」 | 剥がれの配置と、関所をあける判定がズレていた。カードの区間は `from=min(0.46, 0.02+order*0.155)` / `to=from+0.26` で、**3枚とも剥がれ終わるのは進み具合 0.59**。なのに関所は `PEEL_DONE=0.96` まで開かない。この差 0.37 のあいだ、スクロールしても何も進まない（実測: 見終わってから通れるまで **1859ms** の空待ち） | 3枚を区間いっぱいに配り直し（`0.02+order*0.235` / `to=from+0.47` → order2 の終わりが 0.96）。**剥がれ終わり＝関所があく瞬間**で一致させた。実測: 空待ち 1859→**0ms** |
| 2026-09-24 | abashiri v3（値の住み家） | 流れる速さを 0.38→0.62 にしたのに、ブラウザでは 0.22 のままだった | 速さの住み家は4つ（①globals.css の既定 ②パネルの DEFAULTS ③`public/tune-defaults.json` の焼き込み ④ブラウザ保存）。①②とパネル version は直したが、**③の焼き込みに古い 22 が残っていて最優先で上書き**していた。パネルが起動時に `--ev-peel-speed` を書き直すため、CSS の既定は必ず負ける | 焼き込みも 62 に更新。実測: ブラウザが読む値が 0.22→**0.62**、止められる時間 4.34→**1.50秒**。⚠️ 見た目の既定値を変えたら、必ず4つ全部を照合すること（WORKING-RULES 再発防止A） |
| 2026-09-24 | abashiri v3 | スマホの詳細ページでフッターの作字が見えない | フッターの中身は高さ1画面の箱に絶対配置で入れていた。スマホでは中身（作字260px＋サイトマップ3カテゴリ＋SNS）が1画面に収まらず、上へあふれて作字が画面外に出ていた（実測: 作字 -216〜52）。⚠️ 最初 absolute のまま高さだけ外したら、こんどは親の高さが0になり前のセクションと重なった（実測: フッター高さ0・作字 -459） | スマホ(639px以下)では ①中身を絶対配置ではなく【ふつうの流れ】に置いて高さを中身で決めさせる ②作字を 260→150px（倍率も 0.686→0.396 と一緒に変更）③白グラデを最大110pxに抑える ④上に128pxの余白。実測: 作字が「網走市観光サイト」まで画面内に収まる |
| 2026-09-24 | abashiri v3 | 詳細ページのスマホで、ファーストビューと余白が大きく無駄にスクロールする | 写真を出す高さ・白い面までの間・本文の余白を、すべて PC の値のまま dvh で持っていた | ヒデさん指定で スマホは ①ファーストビュー 100dvh→60dvh ②白いグラデ 70dvh→34dvh ③本文の上下 110px→56px ④本文ブロック間 96px→56px。写真は dvh をやめて【比率固定 3:2】に（画面の縦横比で見切れ方が変わらない）。実測: 390/768 とも 60%地点から白、写真の比率1.50、横ずれ0 |
| 2026-09-21 | abashiri v3 | 白背景で音声スイッチの ON側（選んでいない方）のピクトが消える | アイコン icon-bgm-on-dim.svg は【白い丸＋青い線】なのに、SVG 全体に `opacity="0.2"` をかけて薄くしていた。濃い背景では成立するが、白背景では丸も線も地に溶けて見えなくなる。⚠️ 最初この指摘を「選んでいる側（塗られた丸）の話」と取り違えて逆を直してしまった。ヒデさんの「オンがディスエイブルの時」＝**選ばれていない側**のこと | 白背景用に薄がけを外した `icon-bgm-on-dim-light.svg` を作り、`html[data-header-dark="1"]` のときだけ content で差し替え。丸=白(不透明)／ピクト=#0070C9。選んでいる側は指示どおり元のまま |
| 2026-09-21 | abashiri v3 | 詳細ページでは背景が白くなっても音声UI・ナビの色が変わらない | 白背景の切り替えは `html[data-header-dark]` で行う決まりだが、これを立てているのはトップページ（MobileTop）だけで、詳細ページのテンプレには無かった | DetailHeader に判定を追加。画面の上から1/3の位置にある要素の背景色をたどって白かどうかを見る。⚠️ 詳細ページは main が自前でスクロールする箱なので、window ではなく main の scroll を見る。実測: scrollTop 700 以降で白と判定、ナビの字が白→rgb(30,30,30) に変わる |
| 2026-09-20 | abashiri v3 | 調整パネルの構成・見た目が anyflow のルールと別物だった | 網走のパネルには**カテゴリの層が無く**、機能名の節（環境音／人物イラスト…）が直に並んでいた。1つの節に位置・色・アニメが混在。さらに絵文字が29個（タブ名・説明文）、左のインデント縦線あり、位置の呼び方が「上からの距離」「左からの距離」「右端からの距離」と表記ゆれ | anyflow の ANYFLOW-PANEL-STRUCTURE.md を「正」として移植。①tune-panel.js に5カテゴリの層を新設（GRP_ORDER＋grp指定＋名前からの自動判定、中身が無い器は自動で片づけ）②全16節にカテゴリを割り当て ③絵文字29個を全廃 ④border-left を全階層で0に ⑤H1/H2/H3 を 13px700 / 12px600 / 11px400 に ⑥位置を X/Y/Z、余白をギャップ等へ（28件）。⚠️ 作業中に CSS 文字列の連結漏れでパネルごと起動しなくなった（Unexpected string）— 文字列を並べる書き方のファイルでは `+` の付け忘れに注意 |
| 2026-09-20 | abashiri v3 | 「下へ行かせない関所」を作ったのに効かない（scrollTop を書いても戻される） | トップページは独自の慣性スクロールを持ち、毎フレーム `sc.scrollTop = Math.round(posY)` で上書きしている。場面の側から scrollTop を書いても、次のフレームで慣性ループに消されていた | 慣性ループ側が `window.__abashiriScrollGate`（行ってよい上限）を見るようにし、止めたい側はそこに数値を書く方式へ。さらに**関所を張るのが1フレーム遅れて通り過ぎる**問題があったので、場面の3画面ぶん手前から構えるようにした（実測: 一気にスクロールでも取りこぼさなくなった） |
| 2026-09-20 | abashiri v3（調査メモ） | JPEG を WebP に変換しても小さくならないことがある | 公式サイトから取得した写真で実測すると、matsuo-1.jpg は 346KB→391KB と**逆に12%増えた**（notoro-2 は30%減、scene-sango は23%減）。すでに強く圧縮された JPEG を再エンコードすると、WebP でも得をしないことがある | 一律変換はしない。①元が WebP のものはそのまま使う（劣化ゼロ・確実に得）②JPEG の再変換は画素差が 0.7〜1.1% 出るので、ヒデさんの「画質を落とすな」に従い見送り |
| 2026-09-20 | abashiri v3 | KVで「網走市観光サイト」の文字が吹き出しより先に出る | 出だしのブラーの強さが違った。吹き出し（HeroBlurSeq）は timing.kotoba.blur＝**16px** なのに、この文字だけ HE.blur＝**9px** を使っていた。ブラーが弱いぶん早くくっきりし、先に現れて見えていた（実測: 600ms 時点で吹き出しはまだぼやけているのにこの文字だけ読めた）。duration はどちらも1450msで同じ、easingも同じだったので見落としやすい | 時間・ブラー・イージングを吹き出し側（timing2.kotoba）に統一。実測: 200ms刻みの連写で、600ms 時点の両者のぼけ具合がそろった |

## 2026-09-16 サイトの写真枚数を数えたら「0枚」ばかり出る（調査側の失敗）
- **症状**: visit-abashiri.jp の詳細160ページをクロールして写真を数えたら、
  体験以外の117ページが全部「0枚」。「本家に写真が無い」と誤って報告した
- **真因は2つ**
  1. 一覧から拾ったリンクが **相対パス**（`/play/xxx.html`）のままで、
     `page.goto()` が `ERR_NAME_NOT_RESOLVED`。エラーを0枚として集計していた
  2. Playwright の既定 locale が英語なので **`/en/` の英語版へリダイレクト**されていた
  3. 写真は **slick カルーセルの中**（`.m-slider-gourmet__img`）にあり、
     「表示中の img」だけ数えると1枚しか取れない
- **対処**: `new Context({ locale:'ja-JP', 'Accept-Language':'ja' })`／
  リンクは `a.href`（絶対URL）で取る／`main` 配下の `/assets_c/` を全部集めて
  src で重複排除（slick のクローンが同じ src で複数入るため）
- **教訓**: クロールの結果が「一方のカテゴリだけ全滅」なら、まずサイトではなく
  **自分の集計がエラーを0として数えていないか**を疑う。err と 0 は分けて集計する

## 2026-09-16 案を消すと番号が飛ぶ（案1・案3・案11…）
- **症状**: 調整パネルの案を削除するたび、残った案の番号が飛び飛びになり
  「案11」なのに上から3番目、という状態になっていた
- **原因**: 案の名前に番号を**直に書いて**いた（`name: "案11 余白で読ませる"`）。
  番号が「その案の身分証」になっていたので、消しても詰められない
- **対処**: tune-panel.js の pills に `autoNum` を追加。
  `autoNum: "案"` なら残っている案を上から 1,2,3… と数え直して出す。
  `autoNum: { prefix: "階層", style: "alpha" }` なら A,B,C…。
  元の名前に付いている古い番号は正規表現で外してから振り直すので二重にならない。
  内部で保存している値（`detail.pattern` の 11 など）は変えないので、
  焼き込みや保存値はそのまま使える
- **教訓**: 並びが変わりうるものの「見せる番号」は、データに書かず**表示時に採番**する

## 2026-09-16 スクロールで写真を morph させたら「写真が二重に見える」
- **症状**: 全画面の写真を clip-path で縮めて記事の写真へ入れ替える演出で、
  途中で写真が2枚ぶん見えてしまう
- **原因1**: 切り取り先の枠を **%の決め打ち**にしていた。記事はまだスクロール中で
  下にいるので、入れ替わる瞬間に位置が合わない
  → 記事側の写真に ref を付け、`getBoundingClientRect()` を毎フレーム読んで
    その位置へ切り取り枠を合わせる（画面幅が変わってもズレない）
- **原因2**: 同じ要素に `clip-path` と `scale` を両方掛けていた。
  clip は要素のローカル座標に効き、その結果ごと transform で拡大されるため、
  枠のまわりに薄い縁（halo）が出る
  → **切り取りは親・寄りは子**に分ける
- **あわせて**: framer の `useScroll({container, target})` はこのサイトでは
  進捗が正しく出ない（体験セクションでも同じ目に遭っている）。
  `useAnimationFrame` でスクロール量から自分で出すのが確実

## 2026-09-17 Three.js の ShaderMaterial で色が異様に濃く出る
- **症状**: WebGL で写真を貼ると、同じ画像なのに `<img>` より青がどぎつく出る
- **原因**: three は色空間の変換を「自前のマテリアル（MeshBasicMaterial 等）」には
  自動で入れるが、**ShaderMaterial には入れない**。
  テクスチャだけ `SRGBColorSpace` にすると「読む時に linear へ変換 → 変換されないまま出力」
  となり、二度手間の分だけ色がずれる
- **対処**: 読み込み側と書き出し側を**そろえて「変換しない」**にする
  `renderer.outputColorSpace = THREE.LinearSRGBColorSpace`
  `texture.colorSpace = THREE.LinearSRGBColorSpace`
  → 画像のバイトがそのまま出るので `<img>` と同じ色になる
- **ハマりどころ**: `THREE.NoColorSpace` を outputColorSpace に入れると
  three が受け付けず、`.then()` の中で例外 → `.catch` に落ちて
  **エラーも出さずフォールバック表示になる**。定数は存在チェックしてから使う

## 2026-09-17 「消した案を戻す (22)」の数が減らない
- **症状**: 調整パネルで案を消し、コードからも恒久削除したのに、
  「↺ 消した案を戻す」のカウントが減らずに溜まり続ける
- **原因**: 消した控え（localStorage の `:variants` → `hidden`）に **値だけ**を
  残していて、その案がコードから無くなっても控えが残り続けていた。
  ピン留め（fav）と上書きの控え（ov）も同じ
- **対処**: `_vBucket()` で、その行の `item.options` に**もう存在しない値**を
  hidden / fav / ov から捨てて保存し直す。パネルを開いた時点で自動的に 0 に戻る
- **教訓**: 「消したものの控え」は、消せる対象そのものが変わりうる。
  控えを読む場所で毎回「まだ存在するか」を突き合わせる

## 2026-09-17 カルーセルのドラッグが「2回動かすと切れる」／リンクが押せない
- **症状1**: 横ドラッグしても2フレームで止まる。pointermove が2回で pointercancel が1回
- **原因1**: カードのリンク（`<a>`）を掴んだ瞬間に**ブラウザのネイティブな
  ドラッグ＆ドロップ**が始まり、ポインタ操作が横取りされていた。
  `<img draggable={false}>` だけでは足りない
- **対処1**: 入力を受ける箱で `dragstart` を preventDefault する（＋リンクも draggable=false）

- **症状2**: カード内のリンクをクリックしても遷移しない
- **原因2**: `pointerdown` で即 `setPointerCapture` していた。捕捉すると
  **click の対象が捕捉した要素になり**、中のリンクに届かない
- **対処2**: 捕捉は「実際に動き出してから（移動 6px 超）」。
  ただのクリックでは捕捉しないので、リンクが普通に効く

- **合わせて**: `body{overflow:hidden}` のサイトでは、モックページを
  `h-dvh overflow-y-auto` の自前スクロール容器にしないと
  「縦スクロールを奪っていないか」の検証自体ができない

## 2026-09-17 調整パネルの値が一部の部品に届かない（イベントの取りこぼし）
- **症状**: パネルで「カードの縦横比」を 4:3 にしても、カードは 2:3 のまま
- **原因1（取りこぼし）**: パネルは初回に「案を切り替える」イベントと
  「縦横比」のイベントを**同じタイミングで**投げる。案が切り替わって初めて
  その部品が現れるので、生まれた時にはもう縦横比のイベントが流れ終わっている
- **原因2（競合）**: `tune-defaults.json` の読み込みは非同期。パネルの値が
  先に届いていても、あとから焼き込みの値で**上書き**していた
- **対処**: モジュールの側で最後に流れた値を覚えておき、部品が生まれた時に
  そこから読む。焼き込みは「パネルからまだ何も来ていない時」だけ当てる
- **教訓**: 「あとから現れる部品」に値を配る時、イベントだけでは足りない。
  最後の値を持っておくか、常に居る親が持って配る

## 2026-09-17 Figma のフレームの外形から回転角を逆算すると合わない
- **症状**: カンプの重ね写真を再現しようとして、フレームの bbox から
  回転角を逆算したが、実装の輪郭がカンプと合わない（左右で最大70px ずれ）
- **原因**: Figma のフレームは中の画像より大きいことがある（余白を持つ）。
  bbox は「見えている絵」の外形ではない
- **対処**: カンプの**画像を画素で読んで輪郭**（左右の縁・上下の縁を10px刻み）を取り、
  4枚の中心と回転を動かして輪郭のズレが最小になる組み合わせを数値で探した。
  平均ズレ 1.45px まで一致。実装後の再計測でも上端1.6px／左右0.3px以内
- **教訓**: 「カンプ通り」は座標の写しではなく、**見えている形が合っているか**で確かめる

## 2026-09-17 ビジョンの惑星図が Point 01/02 に重なる（「今までは無かった」の再発）
- **症状**: anyflow V5.0 ローカルで、ビジョンの左の Platform 図が右へ寄って Point 01/02 に重なる。V4.0 本番は正常
- **原因**: ブラウザ保存(localStorage)の値がずれていた。図の位置 noPinX 45/noPinY 143 と Point の横ずらし p1X/p2X 0。
  V4.0 本番(焼き込み)は noPinX −95/noPinY 109、Point +36/+70。1回目の対処で「p1X/p2X を 0 に戻す」移行を入れたが、
  0 は 9/15 の一時値で V4.0 の正ではなく、ずれを固定化していた(同じ症状の2回目)
- **対処**: 「正常な版(V4.0 本番)の焼き込み値」を先に読んで正を確定し、その値を params と保存の両方へ焼く移行に差し替え。
  実測: 2000×1108 で図の右端→Point 01 の余白 109px
- **教訓**: 「今までは正常だった」と言われたら、**正常だった版の値を先に取って比べる**。手元の既定値(DEFAULTS)や直前の移行の値を「正」と決め打ちしない

## 2026-09-17 ✏️編集でグラフィックが動かない／要素が選べない
- **症状**: 編集モードで KV のメッシュをドラッグしても動かず、実績の文字や図はクリックしても何も起きない
- **原因**: ①KV の囲み選択(マーキー)用の透明シートがドラッグを横取り ②案をメッシュに固定したのに、描いていない輪の透明な当たり判定が残っていて
  見えない輪をつかんでいた ③実績などスクロール演出の中身は pointer-events:none で素通し設定(実測: 図の上で elementFromPoint が外側の箱を返す)
- **対処**: ①シートの pointer-events を切る ②輪の無い案では当たり判定も隠す ③編集中だけ対象(.tt-hit/.se-hit)に pointer-events:auto を戻す。
  実測: KV ドラッグで kv.gx 17→67、ビジョン図で noPinX −95→−55
- **教訓**: 「つかめない」は elementFromPoint で**実際に何が当たっているか**を見る。透明な当たり判定・素通し設定は目で見えないので推測で直さない
- 2026-09-19 anyflow V5「実績の for SaaS/AI の入場ぼかしが効かない」: 原因＝演出の分岐違い。ぼかしを 24-5 の分岐にだけ入れ、ヒデさんの保存値は 24-4 だった。こちらの Playwright は移行で 24-5 に上書きされるため再現せず、Chrome 本体(ヒデさんの localStorage)で `resFxKey()` を読んで判明。教訓: 案(バリエーション)ごとの分岐に機能を足す時は「今どの案が選ばれているか」を実ブラウザで確認し、共通処理に置く。裏タブは rAF が止まるので計測は `frame()` を手で呼ぶ。
- 2026-09-19 anyflow V5「お問い合わせのグラデで、案で決めた白の中心・広がりが読み込み後に戻る」: 原因＝読み込み時の `cvApplyForm(cvFormKey())`(形=放射/縦/斜め)が FORM_BASE の gcx/gcy/gr を毎回上書き。持ち主を「案(cvSway)」に一本化し FORM_BASE から gcx/gcy/gr を外した。教訓: 同じキーを2つの案系統が持つと後から適用した方が勝つ。値のキーごとに持ち主を1系統に。
