# 🐛 FIXLOG — 不具合の1行台帳（全プロダクト共通）

**使い方（WORKING-RULES 9章 ルールC）:**

1. **不具合の修正に入る前に**、このファイルを症状の言葉で検索する（例: 「真っ白」「回り続け」「変わらない」）
2. 同じ・似た症状が過去にあれば、**対処せず最初から原因調査モード**に入る（真因の列がヒント）
3. 不具合を直したら、**表の一番上に1行追記**する（新しいものが上）
4. 症状は**ヒデさんが言いそうな言葉**で書く（検索で引っかかるように。専門用語だけにしない）

| 日付 | プロダクト | 症状（ヒデさんの言葉で） | 真因 | 対処回数 |
|---|---|---|---|---|
| 2026-09-02 | presenter-notes | サムネイルが出ない/「関数じゃない」エラー（store.js更新が効かない） | store.jsがブラウザキャッシュされ古い版が使われた→vercel.jsonでno-cache＋`store.js?v=2`でバスト | 1 |
| 2026-09-02 | presenter-notes | カンペから「送る」を押してもスライドが動かない/プレビューが同期しない | Figma埋め込みへのpostMessage(NAVIGATE_*)の送信先originが間違い。`embed.figma.com`宛だと黙って無視される→`'*'`宛で解決（受信はclient-idだけで動く） | 1 |
| 2026-09-02 | presenter-notes | 編集エリア・保存ボタンが hidden にしても消えない | `hidden`属性が `.btn{display:inline-flex}`/`.notes-edit{display:flex}` に上書きされていた→ `[hidden]{display:none!important}` を先頭に追加 | 1 |
| 2026-08-30 | anyflow v3 | 導入事例カードが「1枚ずつ」に戻る（再発） | 焼き込み設定ダンプ内の旧cardGap値が、migrationフラグ済みでも復活する構造 | 3回目で根治 |
| 2026-08-30 | anyflow v3 | Visionメッセージの「下からマスクで出る」が消えた | 強み強調の旧str-goが overflow:visible を常時付与しマスクを無効化 | 1 |
| 2026-08-29 | anyflow v3 | 開発者体験2のデザインが「全く変わらない」 | migrationが localStorage へ書き戻し忘れ→リロード1回目だけ直って見え、2回目で保存値に復帰 | 3回目で根治 |
| 2026-08-29 | anyflow v3 | 「強み」のチカチカが見えない | 発火が早すぎて、文字が出きる前にアニメが終わっていた | 2 |
| 2026-08-28 | anyflow v3 | 案を選び直しても前の回転が残って回り続ける | 前の案の保存値（gyroMix/orbitSpin/spinUp）が残留 | 1 |
| 2026-08-28 | anyflow v3 | エコーの残像が全く見えない／つまみが効かない | 実装バグ3件（同日に3連発。エフェクト系は追加時の配線漏れが出やすい） | 3 |
| 2026-08-27 | anyflow v2 | 白画面になる | 「一時停止（直接編集）」の状態が保存され、次回起動で何も表示されない＝保存値の層 | 1 |
| 2026-08-25 | anyflow v2 | すりガラスが透ける・ボケない | Figma書き出しSVGの背景ブラーは`<img>`読込で完全無効 ＋ Chromiumバグ2件（transform/scaleがbackdrop-filterを殺す #993644 / #415354762） | 多数（2日間） |
| 2026-08-23 | 網走 v2 | パネルで直した値が本番に見えない | 古いブラウザ保存値が焼き込み値を上書き → 保存キーをv33に上げて破棄 | 1 |
| 2026-08-23 | 網走 v2 | メッセージ出現がガタつく | スクラブ駆動が原因 → しきい値＋時間トランジションに作り直し | 1 |
| 2026-08-19 | anyflow v1 | 保存（💾）が効かない | 保存まわりの重大バグ | 1 |
| 2026-08-18 | anyflow v1 | 修正したはずが画面に反映されない | ブラウザキャッシュ → HTMLをno-cache設定にして対策済み | 1 |
| 2026-08-15 | anyflow v1 | セクションが真っ白になる | 3回目でやっと真因＝範囲置換の巻き添えで背景CSSごと削除していた | 3 |
| 2026-08-15 | anyflow v1 | 実績→開発者体験の切り替わりに違和感 | 4回目でやっと真因＝暗幕の下で板が2枚動いていた | 4 |
| 2026-08-15 | anyflow v1 | 英字が5%詰まって見える | SF Pro **Display** になっていた（カンプ指定は **Text**）。目視では判別不可＝実測必須 | 1 |
| 2026-08-14 | anyflow v1 | 所定の位置でスクロールしても進まない（空スクロール） | セクションごとのピン留め区間の計算ズレ | 全セクションで発生 |
| 2026-08-12 | 網走 v1 | カルーセルがロックされて動かない（無限ループ） | 再ロック処理の設計 | 2 |

> 出典: 2026-08-30 バイブコーディング白書（8月の全560コミットの棚卸し）を初期シードとして作成。
> 以後は不具合を直すたびにここへ追記していく。
| 2026-09-02 | anyflow v3 | アトム型など複数リング案で「軌道の線の太さ」が無反応(★1/6/7/9等) | 複数リング描画(convDrawShape)がringWidthのみ参照でconvOrbitWidth()の掛け忘れ。swBaseに乗算して解決。サイズ側(convOrbitScale)は元から適用済みだった |
| 2026-09-02 | anyflow v3 | 編集モードの自由回転でXYZを動かすと粒・ドットが全部消える | kvReproのsetHiddenがdotsBackG/dotsFrontGごと非表示にして基本7個だけ再投影(アトム型は再投影も無し)が真因。ドット層は隠さない＋再投影廃止=粒は回転に連動しない仕様へ |
| 2026-09-03 | anyflow v3 | 開発環境dev2カルーセルの入れ替わり時に前面パネルが半透明になり背景が透ける | 前面に入るカードのopacityが左後ろ(0.55)→前面(1.0)へ線形補間で、中間で~0.77と半透明。暗くなる側だけ終盤ディレイの非対称だった。明るくなる側を最初の30%で不透明化する対称処理に。実測: 最前面カード最小opacity 0.775→0.998 |
| 2026-09-07 | presenter-notes | プレゼン（共有）ビューでページを送るとトークスクリプトのフレームまで映る | 送りに `NAVIGATE_FORWARD` を使っておりFigmaプロトタイプの流れに乗っていた（スライドの隣に置いてある原稿フレームへ進む）。検出済みスライド一覧の順で `NAVIGATE_TO_FRAME_AND_CLOSE_OVERLAYS` を直接送る方式へ変更。あわせて present 側の `ensureCard`（未知ノードの自動登録）を廃止＝一覧に原稿フレームが混ざるのを防止、さらに一覧外が表示されたら直前スライドへ引き戻す保険を追加 |
| 2026-09-07 | presenter-notes | カンペのプレビューと共有画面の中身がズレる（Figmaを直しても古いスライドのまま出る） | 原因2つ。①カンペのプレビューが「バックアップ画像があれば画像を使う」設計で、画像は撮った時点の絵なのでFigmaを直すと共有画面（ライブ）と食い違う ②`prefetchImages` が保存済みノードを常にスキップしており、画像が二度と更新されなかった。→①プレビューをライブのFigma埋め込み本軸に変更（画像は8秒無応答時の保険のみ）②ファイルの `lastModified` を控え、変わっていたら全画像を撮り直す |
| 2026-09-09 | anyflow v3 | スマホ(iOS実機)でロゴティッカーが止まる／消えたり出たりする | ①`animationPlayState` が `params.running`(古いlocalStorageで false になりうる)依存 → モバイルは常時 running。②`rv()` が毎フレーム opacity/blur/transform を inline に書き、1826px 幅のマーキー層に blur フィルタが重なると WebKit が合成レイヤーを落とす → スマホでは JS から触らず CSS(opacity:1 / translateZ(0))で固定。headless では再現しない＝実機固有 |
| 2026-09-09 | anyflow v3 | スマホでスクロールすると中身がガクッと動く（揺れ） | iOS Safari のアドレスバー開閉で innerHeight(と幅サブピクセル)が変わり、renderFrame/ResizeObserver/resize の3経路から fit() が再レイアウト。fit() 冒頭で「モバイル＆幅が±2px以内なら return」。scroll 演出の凍結(clock 固定)だけでは止まらなかった |
| 2026-09-09 | anyflow v3 | スマホで実績見出しが Point02 に重なる／ビジョンの絶対配置が section 高さで動く | `.pin-stage` が `top:50%+translateY(-50%)` の縦中央寄せで、中身(1350px)が 780px 箱より高いと上下にはみ出して前セクションへ食い込む。ビジョンの「+42px オフセット」も同根(高さ依存)。モバイルは `html.mb .pin-stage{top:0; transform:scale(var(--sp)); transform-origin:left top}` + `#results` は align-items:flex-start で上端固定 → 値＝カンプY−section上端で置ける |
| 2026-09-08 | anyflow v3 | ビジョンだけ本番が遅い/修正を何度出しても直らない | 真因=世代ガードは「保存世代<焼き込み世代」でしか発火せず、一度「現行世代のまま遅い値」で固まったブラウザは永久に直らない(実測:現行世代+npOrbitDur3.5注入→リロード2回でも3.5)。しかも速化移行のフラグは既に立ち再適用もされない。対策=ビジョン重要値の内容署名を持ち、SHIPPEDと食い違えば本番のみSHIPPED値へ同期+localStorageも書き直す(焼き込みが常に正・固まった環境も次回ロードで自動回復・localhost除外・手動調整は署名一致中は尊重)。世代バンプ不要 |
| 2026-09-09 | anyflow v3 | パネルで「メッシュの線の濃さ」を0.25にしても戻る／SHIPPED_SETTINGSに0.25を焼いても本番が0.5のまま | 真因=起動時 applyOverrideOnBoot→applyGfxVariant が、選択中の案の「上書き控え(gfxVarOverride)」または案定義の conv(lineAlpha:0.5)を再適用し、params/SHIPPED.conv.mesh の値を潰していた（案が持つ値の住み家は params 直下ではなく「案定義／上書き控え／調整の控え(gfxTweaks)」）。対処=上書き控え側(SHIPPED_PRESET_STATE.over と SHIPPED_SETTINGS.gfxVarOverride)の mode.lineAlpha を0.25に、DEFAULTS.conv.mesh.lineAlpha も0.25に統一。検証=メッシュ案6選択で0.25、古い保存値注入→リロード2回で維持 |
| 2026-09-09 | presenter-notes | スライドを並び替えるとトークスクリプト／発表画面が1枚目にリセットされる | 原因3つ。①エディターが常に `edGo(order[0])` で開いていた ②`syncFromFigma` が完了時に `edNode = null` して現在ページを捨てていた（再取得のたび・エディターを開くたびに発生）③`setActiveDeckId` が同じデッキでも `deck-changed` を放送し、発表タブの `location.reload()` とカンペの初期化を誘発（発表中にエディターを開くと再生画面が飛ぶ）。→ ①②見ていたページを `pn_lastpage_<deckId>` に記憶し、並びに残っていれば再開。③同じIDなら放送しない。あわせて端で反対側へ回るループ送りに変更（ライブ／画像モード／エディター共通） |
| 2026-09-10 | presenter-notes | エディター左のサムネイルがFigma修正後も古いまま更新されない | サムネは取得済み nodeId を thumbCache にセッションキャッシュし `need = ids.filter(id => !(id in thumbCache))` で二度と取り直さず、別ファイルの時しか捨てていなかった。Figma画像URLは修正で別ハッシュになるのに古いURLを指したまま。→ syncFromFigma で lastModified の変化を検知したら thumbCache を破棄し直後の loadThumbs で撮り直す（変化なしはキャッシュ維持で無駄打ちしない）。実測: v1→(Figma修正)→v2 に更新、修正なし再取得はv2維持 |
