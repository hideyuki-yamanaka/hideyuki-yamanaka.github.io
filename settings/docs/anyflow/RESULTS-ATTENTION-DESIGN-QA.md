# 追加10案のデザイン・動作確認

確認日: 2026/09/15。対象はローカルモック。新規演出は提案値であり、本番確定値ではない。

## 対象と基準

- [比較一覧](http://127.0.0.1:8777/mock/reveal/)
- 新規10案: URLの内部ID19〜28。表示番号は削除済みの案を除いて再計算する。
- 追加復元: 2回目No.03 → 内部ID18。
- デザインの基準: 既存V4スナップショット`reference.html`。書体はNoto Sans JP／SF Pro Text、本文と実績数値は原文抽出、SVGはS18/A21の元の描画関数。
- 比較元と復元結果を1280px幅のiframe2枚で横に並べた表示を確認。検証ページは確認後に削除。

## 確認結果

| 項目 | 結果・実測 |
|---|---|
| 復元ID18 | 幅1138px、左右71px、見出し38px、主要数字164px、小数字68px、用途見出し27px、本文14px。元の構成と一致 |
| 復元ID18・390px幅 | clientWidth=scrollWidth=390px、主要数字105px |
| 新規10案・PC | 1280×720。全案をSaaSからAIまでスクロールし、見出し・本文の最終位置を確認。横はみ出し0px |
| 新規10案・スマートフォン | 実iframe幅390×高さ844。全案でclientWidth=scrollWidth=390、見出し32px、本文16px・opacity1、SVG2点 |
| 自動タイピングID22 | スクロール約2699pxを保持して1/12文字→12/12文字、本文opacity0→1、blur0pxへ。文字数はスクロール進行度に連動しない |
| 数字の並べ替え | 明示的なslot indexを追加し、20,000+を先頭にしても正しい原本リールを使用。完了後の画面で100+・20,000+・200+を確認 |
| 図の描画 | 原本のSVGと時間ループを再利用。下線削除とトリミング線の補正を維持。新しいラスタ画像は使用していない |
| 開発者体験への接続 | 元の暗転・グラデーションが続く。奥行き案はAI着地後に面の背景を透過させる |
| サムネイル | 同じ実装の描画関数とSVGを使用。確認環境で23枚の生成を確認。独立した近似絵を描いていない |
| 連番・保存 | 既存削除状態を保持したまま2回再読み込み。No.01〜23、ブックマーク0、復元UIなし。内部IDは不変 |
| 構文 | 変更した11のJSモジュールをES moduleモードで構文確認 |

## 発見して修正した点

| 優先度 | 問題 | 修正 |
|---|---|---|
| P2 | 左実績・右説明の案で、右列の幅を全画面用の倍率に使い、文字と図が小さくなった | 右列専用の倍率へ変更。用途見出し35.576px、本文16px |
| P2 | 左列の20,000+が列幅を越える | 数字92.16px、内容幅334.65pxに調整。見出しは読点で改行 |
| P2 | 2列案のピクトと本文が小さく、注目点が弱い | 図の終点枠を540u、本文を最低16pxへ |
| P2 | 奥行きの途中で半透明の2面の文章が重なった | 面を不透明に保ち、横方向の移動距離を広げた |
| P2 | 奥行き案の固定背景がV4の暗転を隠す | 着地後と小画面では面の背景を透過 |

今回の追加部分で未解消のP0/P1/P2の表示不具合はない。演出の好み・読みやすさの最終判断は比較サイトで選ぶための提案段階。

## 保存状態の確認・解消

追加の明示指示「削除したものはこのサイトから完全削除」により、内部ID16は復元せず完全削除した。カタログ・履歴・専用JS/CSS・読み込み参照から除去。空の保存状態／旧削除記録／旧ブックマークを持つ状態のいずれも、23案でID16を解決できないことを分離したNode環境で確認。確認待ちは解消。

## 範囲と制約

画像生成や新しい有料サービスは使用していない。ローカルのモックだけを更新し、公開サイトのデプロイは行っていない。削除はブラウザー内の比較状態として保持し、公開サイトや別ブラウザー、Gitの履歴を削除する処理ではない。
