# Anyflow — スクロールで読む26案（第5回追加・2026-09-14）

## 制作回・バージョン・日時

履歴の登録：**2026-09-14 17:53:25 JST**。画面表示は分まで。リロードや閲覧のたびに作成日時が変わることはない。

| 提案回 | 作成した内容 | 作成記録・JST | 根拠 |
|---|---|---|---|
| 第1回 | 旧01〜10の初回案 | 2026/09/13 21:49 | 旧 `catalog.js` のファイル作成記録 |
| 第2回 | 旧03・04を残し、残り8案を入れ替え | 2026/09/13 22:27 | 旧一覧の「Round 02」表記と `catalog.js` 保存記録 |
| 第3回 | P1〜P5・T1〜T5・R1〜R5、V4全ページの15案 | 2026/09/14 16:03 | 現 `catalog.js` 初回ファイル記録。16:15のGit記録でもこの15案を確認 |
| 第4回 | N1〜N10、新しい構図・視線誘導 | 2026/09/14 17:04 | N群追加時の `catalog.js` 保存記録 |
| 第5回 | TY1、タイピングとスクロール | 2026/09/14 17:53 | 今回、新規登録した日時 |

**第1〜4回は過去のファイル記録から復元した目安で、完成時刻・納品時刻を保証するものではない。** 旧番号の第1・2回は履歴に残す。現在の一覧に掲載するのは第3〜5回の26案。

各カードと各案の下部ナビに、提案回・案ID・管理バージョン・作成記録・内容更新日時を表示。タブ名にも提案回・ID・バージョンを表示する。`history.js` が共通の保存先。

- `v1.0 / v1.1` は今回から付けた**各案の管理番号**で、サイト本体のV4とは別。
- 第3回15案は、共通の初期用途名追加を含む現在の状態を `v1.1`、内容更新は9/14 17:06とする。
- 第4回N群は `v1.0`、内容更新は9/14 17:06。N2の文字位置・N3の案内線位置を調整した2案は `v1.1`、内容更新17:12。
- TY1は `v1.0`、作成登録17:53・内容更新17:57（初回実装のスタイル保存記録）。今回の履歴整理だけで過去の作成記録を書き換えない。
- 今後、新しい案を足すときは新しい回・ID・固定日時を記録する。同じ案の修正は作成記録を保持し、内容更新日時とその案のバージョンを進める。

## 第5回 TY1 — タイピングとスクロール

- 開く: http://127.0.0.1:8777/mock/reveal/demo.html?concept=TY1#results
- 全体の流れから開く: http://127.0.0.1:8777/mock/reveal/demo.html?concept=TY1#vision
- 演出ファイル: `/Users/hideyuki/Developer/Claude Code/anyflow/v4/mock/reveal/typing.js`

まず「事業の推進力を、Anyflowが支えます。」と3つの実績を見せる。実績が下部に収まると、左にピクト・右に説明。for SaaS / for AIは説明の入口から表示する。

1. SaaSの「リアルタイムに」「データ同期」を、スクロール量に合わせて一文字ずつ表示。
2. 見出しが**全文表示された後**に、本文が透明＋ブラー10pxから、不透明＋ブラー0pxへ現れる。
3. 本文が鮮明なまま読む区間を取り、その後AIへ移る。
4. AIの「コンテキスト取得」「から実行まで」でも同じ順序を繰り返す。
5. 通常の縦スクロールに戻り、V4側のグラデーション・開発者体験へつながる。

文字をタイマーで自動再生しない。スクロールを止めると見出しの文字数と本文のブラーも止まる。逆スクロールでは順番どおりに戻る。ピクト内部の既存アニメーションはV4の動作を引き継ぐ。

### 順序と読み取り区間（提案値）

セクション高さ640vh。固定中の進行度 `p` は0〜1。SaaSの場面内進行度 `q=clamp((p−0.17)/0.36)`、AIは `q=clamp((p−0.57)/0.39)`。

| 対象 | 進行度 | 内容 |
|---|---|---|
| 全体 | p=0〜0.12 | 大きな実績を読む |
| 全体 | p=0.12〜0.20 | 数値が下部へ収まり、SaaSの図・用途名が出る |
| 各説明 | q=0.05〜0.45 | 見出しを一文字ずつ。本文は不透明度0 |
| 各説明 | q=0.45〜0.49 | 見出し全文が完成。本文はまだ出さない |
| 各説明 | q=0.49〜0.66 | 本文の不透明度0→1、ブラー10→0px、縦位置12→0px |
| 各説明 | q=0.66〜1 | 見出し・本文を固定して読む |
| SaaS→AI | p=0.53〜0.61 | SaaSが退き、AIが現れる。実績とセクション見出しは保持 |

タイピング中も**最終文字列の領域を予約**する。未表示文字の幅を消さず、文字ごとの不透明度で表示する。改行位置・見出し枠の高さ・本文の開始位置が、文字数によって跳ねないこと。スクリーンリーダーには見出し全文を渡す。

### サイズ・占有率（提案値。下表はコードからの計算値）

`u = min(W/1440, H/850, 1.18)`。この表は今回ブラウザーで実測を終えた値ではない。

| 要素 | 配置・寸法 | 1280×720での計算値 |
|---|---|---|
| ピクト枠 | 中心28%W・49%H、幅600u、高さ468u | 約508×396px、枠幅39.7%W |
| 説明枠 | 中心70.5%W、上端31%H、幅min(41%W,555u) | 約470px、幅36.7%W、上端223px |
| 説明見出し | 48u、ウェイト300、行間150%、字間1.5% | 約40.66px。本文の3倍 |
| 本文 | 16u、ウェイト300、行間195%、字間0% | 約13.55px |
| 用途名 | 22u | 約18.64px |
| 実績数値 | 初期138u→下部52u。ウェイト100 | 約116.89→44.05px |

`for SaaS / for AI` の色、文言、数値、実際のピクトは最新V4側から取得する。ピクトのサイズはSVG枠だけでなく、枠の内側の線の占有率も比較する。

幅800px以下・高さ600px以下・動きを減らす設定では、既存のモック共通の縦流れにする。モバイルでは見出し全文を表示し、文字入力待ちは挟まない。これは今回の提案で、モバイルの確定カンプ値ではない。

## 開く場所

- 一覧: http://127.0.0.1:8777/mock/reveal/
- 各案: `http://127.0.0.1:8777/mock/reveal/demo.html?concept=P1#results`
- Visionから: 上記の末尾を `#vision` にする。画面下の「Visionから」でも移動できる。
- KVから: URL末尾の `#results` を外す。
- コピー元のV4: http://127.0.0.1:8777/mock/reveal/reference.html
- 実装: `/Users/hideyuki/Developer/Claude Code/anyflow/v4/mock/reveal/`

P1〜P5・T1〜T5・R1〜R5、N1〜N10、TY1の合計26案。以前の1〜10案、Claude側の1・4・5・6・10〜19案とは別の番号。既存案を上書きしない。

今回の追加指示：ピクトだけの状態でも用途が分かるよう、初期画面からfor SaaS／for AIを添える。全案を大きくするのではなく、ジャンプ率で主役を強める案と、落ち着いたサイズで視線を導く案を混ぜる。

P1を含め、説明がまだ見えていない図には用途名を表示。説明の用途名が出てくると図側の用途名が薄くなり、読み取り状態で重複しない。PCの初期ラベルは `max(18px, 24 × u px)`。1280×720では20.33px、画面幅の約1.59%。モバイルは20px。

## 新規10案 — 構図と視線誘導を変える

- 新規案の一覧: http://127.0.0.1:8777/mock/reveal/#group-N
- 各案: `http://127.0.0.1:8777/mock/reveal/demo.html?concept=N1#results`
- 新規10案の演出: `/Users/hideyuki/Developer/Claude Code/anyflow/v4/mock/reveal/new-concepts.js`

| ID | 案名 | 新しく試す見せ方 | 大小の扱い | 高さ（提案値） |
|---|---|---|---|---:|
| N1 | 一本の線が、価値をつなぐ | 実績から図・説明へ伸びる線で、視線の通り道をつくる | 最初の大きな実績を上へ縮める | 580vh |
| N2 | 余白がひらく、二つの窓 | 名前付きの図を囲む窓が広がり、説明を含む展示面になる | 枠と余白が広がる。文字サイズは抑制 | 460vh |
| N3 | 読む位置に、しるしを添える | 全情報を見せたまま、細い印が読む箇所を示す | サイズを変えず、位置の案内で訴求 | 500vh |
| N4 | 数字の奥から、価値が現れる | 大きな輪郭数字が背景へ退き、図と説明が手前に出る | 数値264→340 × u px。ジャンプ率を強く使う | 560vh |
| N5 | 一枚ずつ、縦にめくる | 実績・SaaS・AIを縦のフィルムとして送り、場面ごとに読む間をつくる | 各場面は読みやすい大きさを維持 | 640vh |
| N6 | 対角線で、意味が向かい合う | 図と説明を左右交互に置き、ジグザグに視線を進める | 全体が揃う場面の大小は控えめ | 500vh |
| N7 | 数字が通る、水平の帯 | 上の帯に大きな数値、下に落ち着いた図・説明を置く | 上の数値132 × u pxを主役にする | 620vh |
| N8 | 円弧をめぐり、意味に出会う | 三つの実績を円弧上で見渡し、円弧が図を囲む配置に変わる | 実績105→52 × u px。動線と構図を重視 | 580vh |
| N9 | 余白が、見せ場を選ぶ | 境界線で図と文章の領域を交互に広げ、途中で実績を見せる | 初期ピクト枠810 × u px。大きさの差を強く使う | 620vh |
| N10 | 見出しを、栞にする | 左にメッセージと実績を残し、右側を読み進める見開き | 読む位置を安定させ、サイズは保つ | 560vh |

数値の拡大を伴う案も、最後まで文字を動かし続けない。十分に出たところで配置を止め、その区間のスクロール量を読み取りに使う。停止秒数を強制したり、クリックで次へ進ませたりしない。

新規案も、元のV4ページ全体を使う。描画の追加は既存の1本の描画ループ内で処理し、一覧で26個の全サイトiframeを同時に動かさない。

### 新規案の実測例（1280×720）

| 場面 | ブラウザー実測 | 読み取り方 |
|---|---|---|
| P1・初期の用途名 | 20.33px | 図だけの状態でfor SaaS／for AIが読める |
| N4・初期数値 | 223.62px | 同じ案の本文13.55pxに対し約16.5倍 |
| N7・上の数値 | 111.81px | 下の説明見出し35.58pxに対し約3.1倍 |
| N2・左右の説明 | 両方の上端313px | 本文の長さが異なっても用途名・見出しを同じ高さに揃える |
| N3・説明見出し | 32.19px | 大きさを変えず、細い案内線で読む場所を示す |
| N10・左の見出し／実績 | 38.96px／62.68px | 見出しと根拠を常に参照できる |

輪郭数字・背景の薄い面・円弧・案内線は今回の**提案表現**。既存カンプの確定要素ではない。色は現行の灰色・黒・ピンク・青の範囲で構成し、採用時には実装側の最新の色定義へ接続する。

## 何を土台にしたか

2026-09-14の作業開始時点の `anyflow/v4/index.html` を丸ごとコピー。KV、Our Vision、プラットフォーム図、Point 01/02、実績、開発者体験のグラデーションと画面、導入事例、問い合わせ、フッターまで含む。

以前の簡易モックのVision・開発者体験を再利用していない。`demo.html` の既存部分は、以下の4つの追加だけを除くとコピー元HTMLと一致する。

1. ローカルの既存画像を読むための `<base href="/">`
2. このモック専用CSSの読み込み
3. このモック専用JSの読み込み
4. 既存の描画ループからモックを更新する1行のフック

コピー時点の原本SHA-256: `2ec081085e712c56de11a579ef28ffcee434bc0cedda3f754a548209dcb91fe0`

原本と公開アセット97ファイルについて、作業開始時のハッシュと変更後のハッシュが一致した。元の `anyflow/v4/index.html` は編集していない。新しい `/mock/reveal/` 内のファイルだけで実績の演出を試す。デプロイ操作はしていない。

**Figmaの最新値をこの作業で再取得したという意味ではない。** カンプ準拠として管理されている現行V4実装をそのまま土台にした。採用後にClaude側で実装する際も、その時点の最新ソース・アセット・確定値を正とする。

## 既存15案の内容

| ID | 案名 | スクロールで起きること | 区間全体の高さ（提案値） |
|---|---|---|---:|
| P1 | 中央から、二つの意味へ | 二つのピクト→左右に分かれる→説明→実績 | 420vh |
| P2 | 図が上へ、言葉が下へ | 大きなピクト→上に収まる→下に説明。SaaSからAIへ | 540vh |
| P3 | 図を残し、言葉を渡す | 図が左へ→見出しを一行ずつ→本文。SaaSからAIへ | 580vh |
| P4 | ピクトから、横の景色へ | ピクト→横に説明が開く→次の価値へ横移動 | 560vh |
| P5 | 二つの図が、実績を囲む | ピクトが左右へ→中央に20,000+→他の数値→説明 | 440vh |
| T1 | 言葉の余白に、図が現れる | 大きな説明→左に収まる→右にピクト。SaaSからAIへ | 540vh |
| T2 | 説明が上がり、図が立ち上がる | 説明を読む→上に移る→下からピクト。SaaSからAIへ | 540vh |
| T3 | 二つの言葉に、二つの図 | SaaS/AIの説明→それぞれの図が順に現れる→実績 | 440vh |
| T4 | 言葉を追い、図にたどり着く | 説明→隣に図→横移動で次の説明と図へ | 560vh |
| T5 | メッセージから、全体像へ | 事業への見出し→二つの説明→ピクト→実績 | 460vh |
| R1 | 言葉から、三つの実績へ | 大きな見出し→100+／20,000+／200+を順に揃える→説明 | 560vh |
| R2 | 一つずつ、実績を刻む | 一つの数値を大きく読む×3→3数値の一覧→説明 | 700vh |
| R3 | 実績を残して、価値を読む | 3数値を大きく見せる→上部に残してSaaS/AIを読む | 580vh |
| R4 | 実績を道しるべに | 3数値→左側に縦に残す→右側でSaaS/AIを読む | 580vh |
| R5 | 数字から、ページがひらく | 20,000+→周囲に他の数値→下に二つの価値が開く | 480vh |

R1〜R5は、添付の「事業の推進力を、Anyflowが支えます。」と3数値の訴求を主題にした案。P5にも数値を大きく見せる場面を入れている。

クリックやタブ切り替えを挟まずに全内容が読める。画面下の一覧・次の案・ジャンプは**比較作業用のナビゲーション**で、採用サイトのコンテンツ演出には含めない。

## サイズと画面占有率を引き継ぐ

### 共通の計算

以下はFigmaから取得した値ではなく、この比較用に設計した**提案値**。

`W = ブラウザー幅`、`H = ブラウザー高さ`、`u = min(W / 1440, H / 850, 1.18)`。

| 要素 | 指定の目安 |
|---|---|
| メインの横幅 | `min(84vw, 1220 × u px)`。小さな中央カラムに閉じ込めない |
| 1サービスを読む横並び | 図の中心は幅の28〜30%、説明側は70〜76%付近。説明枠は幅の約38〜43% |
| SaaS/AIを並べる場合 | 左右の中心は26.5% / 73.5%。各説明枠は `min(38vw, 490 × u px)` |
| 大きな説明見出し | 44〜58 × u px。本文と同じ大きさにしない |
| 二つ並べる説明見出し | 原則38 × u px。P5/R5の全体を揃える場面は30〜32 × u px |
| 本文 | 原則16 × u px。P5/R5の一覧場面は14 × u px |
| 説明の行間 | 見出し150%、本文195% |
| 説明の字間 | 見出し1.5%、本文0% |
| 3数値を主役にする場面 | 138 × u px、数値ウェイト100、行間108%、字間−4.5% |
| 一つの数値を主役にする場面 | R2:230 × u px、R5開始時:220 × u px |
| 本文と一緒に残す小さい数値 | 48〜76 × u px。主役として見せる場面から段階的に縮める |
| 読み取り中の位置 | 主に画面の10〜88%内。比較ナビに重ならない下余白も確保 |

`pf-art` の幅は**SVG全体の枠**の幅。元のSVGは220×220の中に余白を持つため、線で描かれた図の実寸は枠より小さい。Claude側で別のSVGを使う場合、CSSのwidthだけ同じにしても印象は一致しない。元のviewBoxと線の占有率まで比べること。

### 1280×720で取得したブラウザー実測

以下は書き出し画像のピクセル数ではなく、ブラウザーのCSSピクセル。Retinaの画像サイズと混同しない。

| 場面 | 実測 | 画面幅に対する比率 |
|---|---:|---:|
| R1・3数値の読み取り | 横幅1033px、数値116.89px | コンテンツ80.7%、文字9.1% |
| R1・3数値の読み取り | 見出し38.96px | 3.0% |
| R2・20,000+単独 | 数値194.82px、枠839px | 文字15.2%、枠65.5% |
| P3・SaaS説明 | 見出し40.66px、説明枠466px | 文字3.2%、枠36.4% |
| P3・SaaSピクト | SVG枠491×383px | 枠幅38.4% |
| T1・SaaS説明 | 見出し40.66px、説明枠483px | 文字3.2%、枠37.7% |
| R3・上に保持する数値 | 数値59.29px | 4.6% |
| T5・最後に揃った数値 | 数値44.05px、下端633px | 文字3.4%、下端は高さ87.9% |

数値を20,000+から任意のカウント途中の値へ置き換えない。モックでは3数値とも原本HTMLから読む。ラベル・本文も同様にコピー元HTMLから取得する。

## 動きの仕様

- 進行度 `p = clamp(-実績セクション上端 / (セクション高さ − 画面高さ), 0, 1)`。
- 移動・透明度の区間は `stage.js` に各案ごとに記載。開始点から終了点までを smoothstep で補間する。
- 要素が出たあと、配置が止まる読み取り区間を取る。スクロールを続けなければ次へ勝手に進まない。
- 逆スクロールで位置と出現順が戻る。ホイールやタッチを独自に奪う処理を追加していない。
- 元からあるV4のスクロール処理を利用。別のrequestAnimationFrameループを15案分動かさない。
- ピクト内の線や図形の動きは元の `drawValueIcons()` を使う。元の図形、設定、線幅、速度を引き継ぐ。
- 横移動はP4/T4の**実績セクション内だけ**。Visionを横移動に巻き込まない。
- 実績の固定区間が終わると元の縦スクロールに戻る。元の `devDarkK` を読み、新しい文字の色も実際の暗転と同期させる。
- 開発者体験の背景、グラデーション、エディター、文章出現、次のブロックを再実装しない。元のページの処理を使う。

## 小さい画面

幅800px以下、または高さ600px以下では固定を解除し、通常の縦流れにする。ピクト→説明 / 説明→ピクト / 実績→説明というグループの順序を残す。15案のPCの構図をそのまま縮小して押し込まない。

`prefers-reduced-motion: reduce` でも新しい固定・移動演出を解除する。前後の既存V4セクションの仕様までは書き換えない。

この縦流れは今回のモックの提案。スマホカンプの確定値として採用しない。

## ファイルの役割

| ファイル | 役割 |
|---|---|
| `index.html` | 比較一覧 |
| `catalog.js` | 26案のID・名前・説明・スクロール区間 |
| `history.js` | 提案回・バージョン・作成記録・内容更新日時の固定データ |
| `gallery.js` | 履歴と一覧のサムネイル。全サイトiframeを常時動かさない |
| `demo.html` | V4全ページのコピー＋比較用フック |
| `reference.html` | コピー元のV4全ページ。内容比較用 |
| `detail.js` | 原本の文言取得、実績の差し替え、実際の図形の再利用 |
| `stage.js` | 共通描画・初期用途名・既存15案の配置・サイズ・進行度・色 |
| `new-concepts.js` | 追加N1〜N10の構図・視線誘導・ジャンプ率 |
| `typing.js` | TY1の文字表示と本文ブラー。スクロール量だけで進行 |
| `study.css` | 新しい実績と比較UI専用。前後セクションを変更しない |

## Claude Codeへ渡す文面

```text
以下を読んで、採用する案の「見せる順序・画面占有率・文字と図の大小・読むために止まる区間」を移植してください。

/Users/hideyuki/Developer/Claude Code/settings/docs/anyflow/RESULTS-REVEAL-STUDY.md
/Users/hideyuki/Developer/Claude Code/anyflow/v4/mock/reveal/stage.js
/Users/hideyuki/Developer/Claude Code/anyflow/v4/mock/reveal/new-concepts.js
/Users/hideyuki/Developer/Claude Code/anyflow/v4/mock/reveal/typing.js
/Users/hideyuki/Developer/Claude Code/anyflow/v4/mock/reveal/history.js
/Users/hideyuki/Developer/Claude Code/anyflow/v4/mock/reveal/detail.js

採用案IDは今回指定したものを使い、過去の数字だけの案番号と混同しないでください。
第3回=P/T/R、第4回=N、第5回=TY1です。TY1の場合は、見出しのタイピング完了後に本文をブラーから表示する順番と、上記MDの画面占有率・読むための固定区間を守ってください。
実装の正は、Claude側にある現在の最新V4です。モックのdemo.htmlで本体index.htmlを置き換えないでください。
文言・数値・フォント・色・ピクト内部の図形・既存アニメーション・Vision・開発者体験・グラデーションは、現在の確定データを維持してください。
実績の動きだけを移植し、ここにあるサイズと画面占有率を目安にしてください。図のCSS枠だけでなく、SVGの線が画面の何割を占めるかも合わせてください。
同じブラウザー幅・高さ・進行度で比べ、開始／出現途中／読み取り中／退出／開発者体験への接続を確認してください。
```

## 検証記録

### 第5回・履歴追加（2026-09-14）

- 全JSの構文、26案のID重複なし、全案の固定日時と時系列を確認。
- 文字入力中に本文が出ないことを、場面内進行度0〜1の1001点で検査。開始・終了状態も一致。
- 原本と公開アセット97ファイルはハッシュ一致。`demo.html` や既存V4を今回変更していない。
- ブラウザーで26案の一覧、5回分の履歴、各案の回数・バージョン表示を確認。TY1を一覧から開き、1280×720の開始位置、タブ名・下部ナビの日時をDOMで確認。
- **TY1の途中スクロール・逆スクロール・ブラーの実描画、モバイル表示は未検証。** 続くスクロール操作が自動承認レビューから「Selected model is at capacity」として拒否されたため。別の操作手段への切り替えや拒否操作の繰り返しはしていない。下表の既存案の実測と混同しない。

### 第3・4回までの確認済み範囲

- 合計25案のJS構文チェック。
- 元の97公開ファイルのハッシュ一致。
- `demo.html` が4つの追加以外、元のHTMLと一致。
- PC 1280×720でP1〜P5、T1〜T5、R1〜R3の主要場面を実際にスクロールし、構図とサイズを確認。
- P1で実績から既存グラデーション、黒い開発者体験への遷移を確認。
- 新しい演出コードからブラウザーの保存値を書き換えないことを確認。
- 今回、P1初期画面の用途名を確認。N1〜N10の主要場面を1280×720で実際にスクロールして確認し、N2・N4・N8・N9では別の進行度も確認。
- 前回確認待ちだったR4/R5のPC表示も今回確認。
- **スマホ表示の実機相当確認、古い保存値を入れたリロード検証は引き続き未完了。** ブラウザー操作の自動承認がモデル混雑で停止し、その後の再アクセスも「直前にローカルブラウザー確認が拒否されている」として拒否されたため。PCの確認済み場面とスマホの未確認場面を混同しない。一時的な画面サイズ指定は解除した。

採用時は最新の実装とカンプを再照合し、今回の提案値を勝手に確定扱いしないこと。
